---
description: Compatibility fallback for claiming the next eligible work packet
---

Prefer daemon MCP `work.await_packet`. If MCP is unavailable and the operator
asks for CLI fallback, run `striatum claim-next --session-id $SESSION_ID
--json` and then ack the returned packet via the lease and message ids.
