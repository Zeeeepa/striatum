"""RFC 0013 V1 - bundled web UI assets.

The static SPA at ``src/striatum/web/static/`` is served by
``striatum serve --web``. ``importlib.resources.files`` resolves the
asset paths regardless of whether the package is installed editable
or as a wheel.
"""
