description = "Inspect events, jobs, and packets for a session/run/job"
prompt = """
Run `striatum why <id> --json` for any session, run, or job id to
see the event timeline + work-packet history.
"""
