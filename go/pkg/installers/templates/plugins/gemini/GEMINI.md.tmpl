# Striatum Gemini Extension (v{striatum_version})

This file is loaded as Gemini CLI's context when the Striatum
extension is active.

## Driving a Striatum workflow

1. Connect to the daemon MCP endpoint with the operator-issued token.
2. Call `tools/list`, then `session.register` and `work.await_packet`.
3. Ack the lease, do the work, publish the artifact, and complete through
   MCP `tools/call`.

## Skills

- `skills/striatum-workflow/SKILL.md` — workflow scaffolding
- `skills/striatum-scaffold/SKILL.md` — repo init flow
- `skills/striatum-claim-loop/SKILL.md` — MCP claim → ack → publish
- `skills/striatum-supervise/SKILL.md` — long-lived MCP agent session
- `skills/striatum-recover/SKILL.md` — recovery / stale lease
- `skills/striatum-mcp/SKILL.md` — daemon MCP mutation guidance

## Slash commands (TOML)

- `claim-next` `status` `why` `dashboard` `doctor` — compatibility wrappers
  for the corresponding Striatum CLI diagnostics/fallback verbs.

## Sub-agents

- `agents/striatum-recover.md` — focused recovery role.
