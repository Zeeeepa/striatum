
# GH #10 Workflow Note

This issue is scaffolded with its own captured `SPEC.md` for traceability.
Per `docs/ROADMAP.md`, it is executed as part of the #9-#11 security hardening bundle at
`docs/issues/9/workflow.json` rather than as a separate run.
