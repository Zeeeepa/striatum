Step 2+3 (review_design) per RFC 0025 § 2.2 (codex) and § 2.3 (gemini). Mirror Step 1 architecture. Add codex profile (.codex-plugin/plugin.json + 5 skills + 5 commands + hooks + mcp + README) and gemini profile (gemini-extension.json + GEMINI.md + 5 skills + 5 TOML commands + agents/striatum-recover.md). Update ALLOWED_PROFILES, _build_plan dispatch, _bundle_root user-scope paths. Tests cover both profiles + URL-leak invariant + marketplace co-existence. --profile all aggregates all three.
