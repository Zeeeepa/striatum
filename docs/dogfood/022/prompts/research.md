Map: chat_provider.py current shape; Anthropic Messages tool-use docs (tools array, tool_use content blocks, tool_result fed via user role); OpenAI Chat tools-array + tool_calls + role:tool response; tool execution surface (path safety, striatum.api.invoke, git subprocess); briefing inputs (git log, AGENTS.md, status). Plus the two ride-along fixes (graph-node route tolerance — already in working tree; doctor template — references doctor.checks but shape is doctor.problems + doctor.problem_records).
