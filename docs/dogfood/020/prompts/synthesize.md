# Synthesis: lock RFC 0022 V1

Pin:
1. Template tree + per-template content.
2. Route table (history-API URLs).
3. CSS architecture: variables, dark mode, component refresh.
4. SVG layout algorithm (layered Sugiyama-ish).
5. SVG node + edge rendering rules + click navigation.
6. Hash-route redirect mechanism.
7. Mutation button preservation.
8. Test plan.

Deliverable: `DESIGN_SYNTHESIS.md`.
