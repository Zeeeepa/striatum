Pick form-serialization architecture. Three options: (A) structured form fields like jobs[0][id]; (B) JS island that serializes form state to JSON; (C) JSON textarea (fallback). Survey validate_workflow error shape — does it return field paths usable for inline rendering? Confirm CSP allows fetch() POST with application/json from script-src 'self'. Test precedent for the JSON-POST endpoint pattern.
