Research RFC 0025 V1 Step 1 (claude_code plugin). Map RFC 0015's `skills/install.py` architecture: plan-build → render → manifest-compare → write/refuse. Identify reusable pieces (manifest format, edit-detect, --force/--dry-run). Confirm claude_code plugin bundle layout from RFC § 2.1: `.claude-plugin/plugin.json`, `skills/striatum-*/SKILL.md` (5), `commands/*.md` (5), `hooks/hooks.json`, `.mcp.json`, `.manifest.json`, `README.md`. Decide: separate `plugins/install.py` module vs extension of `skills/install.py`.
