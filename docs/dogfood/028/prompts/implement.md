Implement V1 Step 1. (1) `src/striatum/plugins/__init__.py`, `src/striatum/plugins/install.py` (mirror skills/install.py shape). (2) Templates under `src/striatum/plugins/templates/claude_code/`: `plugin.json.tmpl`, `skills/{workflow,scaffold,claim-loop,supervise,recover}.md.tmpl` (reuse skills bodies), `commands/{claim-next,status,why,dashboard,doctor}.md.tmpl`, `hooks/hooks.json.tmpl`, `mcp.json.tmpl`, `README.md.tmpl`. (3) `striatum.cli.plugin_install` module + `striatum plugin install/uninstall` parser stanza + dispatch. (4) `striatum init --with-plugins`. (5) Doctor checks `plugin_missing`/`plugin_outdated`. (6) Tests covering: claude_code profile renders the full tree, manifest written, idempotent re-install, edit-detect refusal, --force overwrite, --dry-run, no URLs in rendered output. (7) Defer Steps 2-3 (codex/gemini) to follow-up dogfoods.
