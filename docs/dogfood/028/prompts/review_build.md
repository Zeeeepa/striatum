Devils advocate build review. Verify: claude_code bundle has all 13 files; second install is byte-identical; edit-detect refuses overwrite without --force; --force overwrites; --dry-run prints plan + writes nothing; manifest schema version + sha256 entries; init --with-plugins integrates cleanly; doctor surfaces plugin_missing + plugin_outdated; no URLs/external paths in rendered output.
