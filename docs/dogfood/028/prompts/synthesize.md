Pin: `striatum.plugins.install` module mirrors `skills.install` (plan/render/manifest); `MANIFEST_SCHEMA_VERSION = "striatum.plugins.manifest.v1"`. CLI: `striatum plugin install --profile claude_code [--scope] [--namespace] [--force] [--dry-run] [--with-marketplace] [--target]`. `striatum plugin uninstall --profile <id>`. `striatum init --with-plugins [profile]` mirror of `--with-skills`. Doctor: `plugin_missing`, `plugin_outdated`. URL-leak invariant via test that walks rendered output.
