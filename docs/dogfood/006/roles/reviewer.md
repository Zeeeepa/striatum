# Reviewer Role (Dogfood 006)

Fresh-context review. Verify D020 (no remote serving) and D006 (api.invoke
is the single dispatch path) are preserved by construction; verify the
test plan covers SSE replay, non-loopback refusal, mutation gating, and
auth. Submit a structured verdict.
