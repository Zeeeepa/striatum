Research V4. (1) Pause/resume: column on runs (`paused_at` + `paused_reason`) vs full new state? Decision criteria: claim-next gate placement, idempotency, recovery interaction. (2) Per-job cancel: existing `recovery.cancel_job` is the right mutation — confirm wrapping for web/CLI mutation surface. (3) Retry: which job states are retriable (failed, canceled, blocked?); lease cleanup pre-retry; queue re-enqueue.
