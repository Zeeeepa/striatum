Pin: Migration v11 adds `paused_at`/`paused_reason` columns to runs. `pause_run` mutation sets them; `resume_run` clears. Claim-next gate: SELECT only runs where `paused_at IS NULL`. CLI: `striatum run pause/resume`. HTTP: `POST /run/<id>/(pause|resume)`. Per-job cancel web wrapper: `POST /run/<id>/job/<jid>/cancel` calls existing `recovery.cancel_job(cascade=True)`. Retry: new mutation `retry_job(conn, *, run_id, job_id)` for failed/canceled jobs — clears terminal state, re-enqueues. CLI: `striatum run retry-job`. HTTP: `POST /run/<id>/job/<jid>/retry`.
