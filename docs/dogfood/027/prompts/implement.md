Implement V4. (1) Migration v11 in migrations.py: ALTER TABLE runs ADD paused_at TEXT, paused_reason TEXT. Update schema.py baseline. (2) `pause_run`/`resume_run` in db.py. Idempotent. (3) Claim-next gate: in db.py `claim_next_work` (or wherever it lives), filter SELECT by paused_at IS NULL. (4) Web routes: POST /run/<id>/pause, /run/<id>/resume. (5) CLI: striatum run pause/resume. (6) UI Pause/Resume buttons on run detail. (7) Web cancel-job route + button on job detail page (calls existing recovery.cancel_job). (8) `retry_job` mutation in db.py — accepts failed/canceled, clears terminal state, re-enqueues. CLI/HTTP/UI. Tests + lint + typecheck.
