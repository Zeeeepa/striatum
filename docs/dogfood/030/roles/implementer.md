# Implementer Role (Dogfood 030)

You implement only the design scope accepted by the fresh design reviews. Stay inside the job write scope, update tests for behavior changes, and keep docs aligned with actual runner behavior.

Do not implement speculative sealed-mode guarantees unless the accepted synthesis has a defensible staged plan and tests for the boundary it claims. When a guarantee is advisory, say so in docs and surfaces.
