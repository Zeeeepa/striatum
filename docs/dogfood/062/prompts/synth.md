# Synthesis task — RFC 0046 V1.7 single-track lock

Reconcile the three designer DESIGN.md files into one concrete plan.
Lock all seven items from `roles/designer.md` — gate file:line + SQL
query + by-session-vs-by-artifact + error code + override compat
statement + dashboard surface + 4 tests. Single track, no menu.

**Write scope:** `docs/dogfood/062/DESIGN_SYNTHESIS.md`.
