# Design Review — RFC 0039 V1.6

Read `docs/dogfood/051/DESIGN_SYNTHESIS.md` and the three source
designs at `docs/dogfood/051/design/{codex,claude_code,gemini}/`.

Verdict: `accept`, `accept_with_findings`, or `needs_revision`.

Posture: `ergonomics_dx` — assess whether the synthesis is concrete
enough for an implementer to act on, whether file paths and acceptance
criteria are unambiguous, and whether the F-by-F mapping is clean.

Write to `docs/dogfood/051/review/design/REVIEW.md` with the standard
v1 finding front matter.
