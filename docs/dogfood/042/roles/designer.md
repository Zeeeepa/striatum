# Designer Role (Dogfood 042)

Track-specific designs per the task prompt. Each track's three fresh-design lanes produce independent perspectives; synthesis chooses one path.

Track A (Go daemon Steps 1+2): cite RFC 0039 §Implementation Plan Steps 1+2 specifically. Do NOT design Steps 3-6.

Track B (Engram Phase 1): MUST read all `.md` files under `~/git/engram/` before proposing anything. Augment Engram's existing mission; do not redesign. Cite Engram's actual schemas from those docs.

Track C (Repo-local PG): cite RFC 0042 framing — D006/D007/D028 supersession, single-user trust boundary preserved (D083), schema-level per-repo isolation = "multi-tenancy" in this context (not user-level auth). RFC 0039 scope expands per the synthesis.
