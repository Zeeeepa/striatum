Map service.py routes; supervisor primitive (RFC 0009);
stdlib HTTP options for outbound streaming SSE (http.client +
manual SSE parsing vs adding httpx). Confirm BroadcastChannel
is fine for cross-tab "add to chat" without CSP impact.
Deliverable: research/CHAT_SHAPE.md.
