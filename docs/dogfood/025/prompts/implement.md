Implement V2. Ship: (1) POST /workflows/run/<path> route — mutation-gated, calls run.prepare + run.start, returns {run_id}; (2) If-Match: <sha256> header on POST /workflows/edit/<path>; 412 on mismatch; missing header treated as opt-out; GET stamps disk sha into a hidden script tag; (3) field-level errors — extend validate_workflow path to yield `[{field_path, message}]`; service.py serializes the list into the 422 body; editor JS highlights fields. Tests + lint + typecheck + smoke.
