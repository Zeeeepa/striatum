Pin: POST /workflows/run/<path> (mutation-gated; 405 without --allow-mutations; calls run prepare + run start; returns {run_id} on 200; 422 on validate_workflow failure). If-Match header on POST /workflows/edit/<path> — returns 412 when client's sha doesn't match disk; missing header is allowed for V1.5 backward compat. Per-field validation errors — wrap WorkflowError or extend it; editor renders inline. Test plan.
