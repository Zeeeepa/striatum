Research V2. Confirm: (1) `striatum run prepare` + `run start` CLI surface; idempotency; what bumping `repo_root` looks like for a workflow.json that lives at an arbitrary repo-relative path. (2) `validate_workflow` error shape — does WorkflowError carry a field path or just a string? Survey call sites; propose minimal change to yield `[{field_path, message}]`. (3) sha256 capture surface — where in `_render_workflow_edit_page` to compute disk sha and inject as `data-sha256` so the editor echoes it on POST as `If-Match`.
