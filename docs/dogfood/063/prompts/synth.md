# Synthesis task — RFC 0053 Phase B single-track lock

Reconcile 3 designer DESIGN.md files into `DESIGN_SYNTHESIS.md`.
Lock all 7 items + decide the `escalation` artifact-kind scope
(in this dogfood, or deferred to 064).

**Single track.** Sub-agents per layer (validator / generator /
upgrade / CLI text / PG state / docs / tests) all land in the same
implement job.

**Write scope:** `docs/dogfood/063/DESIGN_SYNTHESIS.md`.
