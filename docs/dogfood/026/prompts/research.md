Research V3. (1) Map run states (`prepared`, `ready`, `running`, `completed`, `needs_branch_confirmation`, ...) and valid transitions; identify where to add `cancelled`. (2) Audit `branch_confirm` (cli/mutations.py:38) — what error fires when target branch exists; where to inject auto-suffix; how `dispatch.py:262` auto-confirms. (3) Survey lease/job cleanup needed when cancelling a running run: leases, queue, inflight jobs.
