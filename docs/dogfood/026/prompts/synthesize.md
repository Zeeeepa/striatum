Pin: `cancel_run(conn, *, run_id, reason=None) -> {state}` mutation; allowed source states; how it affects in-flight leases + queued/running jobs. CLI surface: `striatum run cancel --run-id <id> [--reason]`. POST `/run/<id>/cancel` (mutation-gated). UI Cancel button on the run page when `state in ('ready', 'running')`. Auto-branch suffix algorithm: `<suggested>-<short_run_id>` when target exists. Re-run button on workflow detail page (POST `/workflows/run/<path>` already does this; the button is just sugar). Test plan.
