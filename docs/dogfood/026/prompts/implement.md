Implement V3. Ship: (1) `cancel_run` in cli/mutations.py + CLI subcommand `striatum run cancel`; transitions running/ready → cancelled, voids active leases, marks running jobs cancelled, emits `run.cancelled` event. (2) POST /run/<id>/cancel; mutation-gated; calls `cancel_run`. (3) Cancel button on run_detail.html (rendered when state ∈ {ready, running}); islanded JS in /static/run_cancel.js. (4) Auto-branch suffix in run-now path: when `branch.mode == auto` and the suggested branch already has a confirmed run, append `-<short_run_id>` (first 8 hex of run_id). (5) Re-run button on workflow_detail.html (renders for every valid workflow; same POST /workflows/run/<path>). Tests + lint + typecheck + smoke.
