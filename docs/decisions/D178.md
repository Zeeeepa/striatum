# Proposed decision-log entry for RFC 0119

> **⚠️ ID COLLISION — paste this as `D179`, not `D178`.**
> RFC 0119 and the commit prompt name the new decision "D178", but `D178` is
> already an accepted decision (`docs/decisions/decision-log.md:36` — the
> RFC 0117 `striatum worktree gc` surface; `D178` is the latest assigned ID).
> This file lives at `docs/decisions/D178.md` only because that is the path the
> committer job's write scope mandates. The decision's canonical ID is **`D179`**
> (the next free ID). When merging into `docs/decisions/decision-log.md`, paste
> the row below as **`D179`**.

## Gate provenance

This entry records the outcome of the RFC 0119 ratification gate
(run `run_b089555bd70cd2dc2dc1d13c3cc35b53`): adjudicator verdict
`accept_with_findings` with binding constraints C1–C4
(`docs/rfcs/0119/adjudicator/COLLABORATION_LEDGER_cycle_1.md`). See
`docs/rfcs/0119/ACCEPTANCE.md` for the committer synthesis.

**Merge gate:** land this row **only together with** the RFC 0119 amendment that
discharges C1–C4 and the `proposed → accepted` status flip of
`docs/rfcs/0119-warm-tier-memory-boundary.md` (+ its `docs/rfcs/README.md` row).
The ledger requires C1–C4 to be discharged "before a decision record is filed or
Go implementation begins."

## Ready-to-paste row (assign ID `D179`)

| ID | Status | Decision | Reason | Consequences | Revisit Trigger |
| --- | --- | --- | --- | --- | --- |
| D179 | accepted | Accept RFC 0119 (warm-tier memory boundary + striatum-native read-only hot tier) **with binding constraints C1–C4** from the `run_b089555bd70cd2dc2dc1d13c3cc35b53` ratification gate (adjudicator verdict `accept_with_findings`; branches `state_transition_dependence` / `durable_provenance` / `export_class` all `cleared_with_constraints`). Authorize, **conditional on C1–C4 landing in the RFC text first**: (1) a `spec.md` §"Warm-Tier Memory" pinning three axes — canonical authority (never the warm tier), index scope (everything, durable provenance included), eviction scope (only run exhaust + unsynthesized intermediates leave the git tree); (2) an optional, default-off, redacted `lane_trajectory` corpus-export feedstock class, pull-only and deterministic; (3) a read-only `RecallMemory` projection (Postgres FTS over the daemon's own immutable artifact stream, no pgvector/ML) feeding a scaffold-time digest into new worktrees + the operator bootstrap packet, fail-soft to an empty shelf with an honest provenance header, named `recall.*` (capability `read`, `single_repo`) and **never** `memory.*`. The three corpus invariants are retained and generalized to any external memory consumer. | The two falsifiers' decisive objections were verified and binding, but dischargeable without weakening the corpus invariants: the hot-tier read can move off the claim/worktree-create transactions and the exhaust policy can become a strict allow-list aligned with accepted RFC 0072. The RFC's central "not a state transition" framing was false at its named seams (`buildPacket`@`claim.go:229` runs inside the claim `tx`; `HandleWorktreeCreate` renders between the physical `git worktree add` and the `job_worktrees` row insert), and its only worked exhaust definition evicted `operator_report` / `*_ledger` kinds that accepted RFC 0072 and `spec.md` pin as git-tracked durable provenance. | No `go/` change lands at acceptance. RFC 0119 MUST be amended to discharge all four binding constraints before this row is merged and before implementation begins: **C1** hot-tier recall on a separate connection (never the claim `tx`), digest write fail-soft outside the transition, plus a guardrail that injects a recall-read failure and asserts claim + worktree-create still commit; **C2** worktree-create digest render ordered + fail-soft so a render failure cannot orphan a git worktree without its `job_worktrees` row; **C3** exhaust defined as an explicit per-kind allow-list (strict subset of RFC 0072's blob set, **never a glob**) excluding `operator_report`, `decision`, `escalation`, `operator_brief`, `work_plan`, `collaboration_ledger` and every corpus-export durable-provenance kind, with RFC 0119 explicitly reconciling with / superseding RFC 0072's boundary table; **C4** `lane_trajectory` explicitly superseding the enforced `validateCorpusSourcePath` transcript-denial guardrail and **D028**, and either defining a deterministic redaction-normalization-to-bytes contract or staying outside the corpus durable-provenance bundle. Implementation (`RecallMemory` + scaffold injection + the export class) is the operator's follow-up, gated on the corpus guardrails staying green (no `memory.*` in the registry, no external-consumer import, all state transitions green with the warm tier absent). | Revisit if any C1–C4 amendment cannot be made without widening the corpus invariants; if the eviction allow-list proves too coarse (the RFC's per-kind-vs-fixed-list open question); if scaffold injection should enrich the operator bootstrap packet before lane worktrees; or when a later decision supersedes the warm-tier boundary. |

## C1–C4 binding-constraint discharge checklist (operator follow-up before merge)

The RFC text must be amended to discharge each constraint; the decision row above
must not be merged until all four are green.

- [ ] **C1 — `C1-HOT-TIER-SEPARATE-CONNECTION`** (state_transition_dependence /
  critical). Amend RFC D3 (`:88`–`:99`): recall read on a **separate connection**,
  never the claim/worktree-create `tx`; digest write fail-soft outside the
  transition; drop the "not a state transition" framing. Gate: inject a
  recall-read failure and assert claim + worktree-create still commit.
- [ ] **C2 — `C2-WORKTREE-ORPHAN-GUARD`** (state_transition_dependence / high).
  Amend RFC D3's worktree-create seam: order the render relative to the
  `job_worktrees` row so a render failure cannot orphan a git worktree. Gate:
  inject a digest-render failure during worktree create; assert no unrecorded
  worktree remains.
- [ ] **C3 — `C3-EXHAUST-ALLOWLIST`** (durable_provenance / critical). Replace
  the RFC Open-Questions glob (`:137`–`:141`) with an **explicit per-kind
  allow-list**, strict subset of RFC 0072's blob set, **never a glob**, excluding
  `operator_report`, `decision`, `escalation`, `operator_brief`, `work_plan`,
  `collaboration_ledger` and all corpus-export durable-provenance kinds; RFC 0119
  reconciles with / supersedes RFC 0072's boundary table.
- [ ] **C4 — `C4-LANE-TRAJECTORY-CONTRACT`** (export_class / high). Amend RFC D2
  (`:77`–`:86`): name the supersession of `validateCorpusSourcePath`
  (`redaction.go:24-29`, `:84-87`; `exports.go:408`) and **D028**; specify a
  deterministic redaction-normalization-to-bytes contract, or keep
  `lane_trajectory` out of the corpus durable-provenance bundle.

## No Go code in this run

This run files the acceptance synthesis and this proposed entry only. No `go/`
change lands. `RecallMemory` + scaffold injection are the operator's follow-up,
gated on C1–C4 being discharged and on the corpus guardrails staying green.
