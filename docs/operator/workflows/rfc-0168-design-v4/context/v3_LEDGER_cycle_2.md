---
schema_version: "striatum.collaboration_ledger.v1.1"
artifact_kind: "collaboration_ledger"
shape: "falsification_gate"
topic: "RFC 0168 P0 — per-lane OS uid as the lane security principal: v3 REVISION re-attack, cycle 2 (re-adjudication of the unchanged v3 Holder against the second-round falsifiers) — discharge the two v2 residual holes (C1-RESIDUAL OQ2-SCRUB-POSTCONDITION process predicate, C2-RESIDUAL OQ4-ACL-PROVISIONING-TRANSITION) while carrying the v1-proven hard core HC-A1..A5 and the v2-credited C1 four-state lease machine + C2 final .striatum/-excluding ACL end-state + OQ1/OQ3/OQ5/OQ6 + narrowing invariant forward unregressed"
participants:
  - "holder-author-002"
  - "falsifier-reviewer-003"
  - "falsifier-reviewer-004"
  - "adjudicator-author-002"
cycle: 2
entries:
  - kind: claim
    by: "holder-author-002"
    refs: ["dialogue:1"]
    text: "v3 REVISION of the RFC 0168 P0 falsification_gate spec (unchanged from cycle 1: holder/1 blob a34a7cc4), revising the v2 HOLDER to DISCHARGE the two v2 cycle-1 residual constraints while carrying everything v1 AND v2 cleared forward unregressed — a tightening of TWO predicates, not a rewrite. C1-RESIDUAL (OQ2-SCRUB-POSTCONDITION): the scrub postcondition predicate P1 is restated as a FAIL-CLOSED three-way classification — tolerate ONLY true zombie/dead tasks (/proc state in {Z,X,x}, or a PID that has fully left the re-enumerated domain), and BLOCK on every other observed state (R/S/D/T/t/I/W/P/K/…, any unrecognized state char, AND an unreadable/ambiguous /proc read for a still-present PID), finalizing quarantined with a typed lane_uid_scrub_failed. P1 is wired to a NEW classifier classifyPoolUIDTaskState(pid)->{Reaped,Live,Unknown}, explicitly NOT the binary Z-only processZombie (tmux_liveness.go:599-614, which returns not-zombie on a read error); observed PIDs + /proc state chars + classification are recorded in scrub_proof (tolerated Z residue) and scrub_failure (the blocking non-zombie PID+state), with new negative test A21 TestStoppedOrTracedUIDProcessBlocksReturn. C2-RESIDUAL (OQ4-ACL-PROVISIONING-TRANSITION): the allowlist / exclude-at-traversal provisioning form is made MANDATORY (the group grant NEVER applies g:striatum-lanes:rX or its default to .striatum/, .git/, or provider/token-cache paths, even transiently; no default ACL on <repoRoot> itself), the raw setfacl -R …:rX <repoRoot>-then-strip live-repo path is EXPLICITLY FORBIDDEN, A16 is extended into the transition test A22 TestPoolACLProvisioningNeverTransientlyExposesScratch (an unleased AND a different-leased pool uid attempt open(2)/traversal before/during/after provisioning; no read ever succeeds), and a deterministic pure ACL-planner guard + unit test A23 TestACLPlannerRejectsRawRecursiveRootWhileScratchExists fails any g:striatum-lanes op that targets <repoRoot> (or any ancestor of .striatum/) as a raw recursive root while .striatum/ exists. The holder asserts (and marks CONFIRMED, citing mcpconfig.go:241/266) that the 0600 MCP bearer already lives under .striatum/scratch/<supervisor_id>/, where loop.go writes pty.log; the v3 final scratch ACL pushes .striatum/scratch to traverse-only --x and grants rwx only on .striatum/scratch/<supervisor_id>/. Carries HC-A1..A5 (Part 1 re-verified vs pty.go/tmux_liveness.go HEAD), the v2-credited C1 durable four-state lease machine and C2 final .striatum/-excluding GROUP ACL end-state, OQ1/OQ3/OQ5/OQ6, and the narrowing invariant forward unregressed. Build-run target: runtime migration 0046+ (0045 reserved by RFC 0170 P0 cullable_entity; not hardcoded) + owner-bundle bump owner/0023+ for striatumd.lane_uid_leases."
  - kind: challenge
    by: "falsifier-reviewer-003"
    refs: ["dialogue:2"]
    correspondence: not_material
    text: "C1 scrub-postcondition lens (second-round re-attack) — NO material challenge lands; C1-RESIDUAL is GENUINELY CLOSED from this lens. The v2 OQ2-SCRUB-POSTCONDITION defect was real (v2 P1 blocked only R/S/D and let a T/t stopped/tracing-stopped non-zombie pool_uid task reach returned and re-lease dirty state; v2_HOLDER.md:222, v2_COLLABORATION_LEDGER_cycle_1.md:21). The v3 holder tightens exactly that predicate and no remaining non-zombie pool_uid survivor can still pass P1. (a) Stopped/tracing-stopped survivor: T/t no longer pass — v3 classifies recognized non-dead states including T and t as Live, and Live blocks to quarantined (HOLDER.md:303); A21 is concrete enough for the build contract (inject the stopped/traced uid-owned task, assert classifier=Live, finalize quarantined+lane_uid_scrub_failed, prevent allocation, preserve quarantine across boot-epoch rotation, clear only through the same P1-P5 proof after the domain is clean; HOLDER.md:558). (b) Reparented/detached survivor: P1 is uid-domain based, not parent-tree/tmux-session based — it enumerates ALL pool_uid-owned PIDs via /proc/<pid>/status and classifies each, so a daemonized/reparented/detached task still owned by the pool uid stays in the enumerated domain and is Live/Unknown unless truly Reaped (HOLDER.md:214). (c) Re-fork after kill before proof closure: bounded re-kill/re-probe re-enumerates before classifying; a stable non-zombie survivor blocks, a PID is tolerated only when it has fully left the re-enumerated domain (HOLDER.md:210,:308). Classifier wiring: the old binary processZombie still exists exactly where pinned (tmux_liveness.go:599: reads /proc/<pid>/stat, takes the field after the last ), returns true only for Z, false on read error) and is inadequate as a proof predicate; v3 does NOT wire P1 to it — it specifies a NEW classifyPoolUIDTaskState 3-way with Reaped only for Z/X/x, Live for recognized non-dead incl. T/t, Unknown for unreadable/malformed still-present PIDs or unrecognized chars, plus a spec note + A21 preventing a processZombie / !processZombie implementation (HOLDER.md:293,:314) — satisfying the packet non-binary-classifier requirement. Proof record: scrub_proof records observed PIDs + /proc state chars + classification; scrub_failure records the blocking PID + non-zombie state, so doctor distinguishes tolerated Z residue from a quarantine cause (HOLDER.md:140,:243). Carry-forward: NO C1 regression — four-state machine, held-unique index, three-transaction boundary, failed-proof-quarantines, leaked-active/stuck-scrubbing reaper, quarantine-survives-restart, dirty-excluding exhaustion (free=N−active−scrubbing−quarantined), OQ3/OQ5/OQ6, narrowing invariant all intact (HOLDER.md:100,:140,:170,:187,:256,:261,:283,:320,:441,:453,:470,:506). Strongest objection (A21 is still a design-time test name, not code) is not a phase defect — the holder is a falsifiable build spec and A21 names the injected condition, classifier result, transition, non-allocation, restart persistence, proof-gated clear, and recorded /proc evidence. Bottom line: no material C1 falsifier; the v3 holder closes the prior scrub-postcondition gap, no regression."
  - kind: challenge
    by: "falsifier-reviewer-004"
    refs: ["dialogue:3"]
    correspondence: landed_unrebutted
    text: "C2 ACL provisioning-transition / scratch-path lens (second-round re-attack) — STANDING material, source-grounded challenge; C2-RESIDUAL is NOT genuinely discharged. The PROCEDURE half of the v3 C2 fix is genuine and CREDITED: the allowlist / exclude-at-traversal form is mandatory, the live-repo raw setfacl -R …:rX <repoRoot>-then-strip path is explicitly forbidden, and A23 is the right kind of planner guard for the raw-recursive-root case — NO challenge survives against THAT part. The remaining blocker is more exact and source-true: the new C2 final state AND the A22 transition proof are not source-true for the live MCP bearer path. The holder moves scratch-root access from rwx to traverse-only and says the lane's own rwx grant lives at .striatum/scratch/<supervisor_id>/, where mcpconfig.go writes the 0600 bearer and loop.go writes pty.log (HOLDER.md:386-393,:392). Current source does NOT do that for the Claude MCP config: writeEphemeralMCPConfig (go/pkg/agentloop/mcpconfig.go:550-580) sets dir := filepath.Join(repoRoot, '.striatum', 'scratch'), falls back to /tmp only if that path is missing or not a directory, then calls os.CreateTemp(dir, 'lane-mcp-config-*.json') and chmods it 0600 — there is NO STRIATUM_SUPERVISOR_ID component in that path. The line pair the holder cites as CONFIRMED proof of a supervisor-scoped bearer is not that writer: mcpconfig.go:241 writes the gemini settings file (error string 'write gemini settings'), and :266 assigns .striatum/scratch/<supervisorID> only for legacy gemini project-settings backup/created markers — it does not move the Claude bearer. Only the pty.log half (loop.go:139-145,:298-300) is source-true. The current test surface still encodes the root-scratch shape: mcpconfig_test.go:107-128 asserts the Claude config path merely has prefix <repo>/.striatum/scratch (not <repo>/.striatum/scratch/<supervisor_id>/), and TestRewriteEphemeralMCPConfigSwapsEndpointAndToken calls writeEphemeralMCPConfig after creating only .striatum/scratch (mcpconfig_test.go:163-170); the focused tests pass — confirming the current surface accepts root-scratch placement. The launch path explains why this matters: scratch_acl.go:31-48 grants .striatum --x and .striatum/scratch rwx + default ACL EXACTLY because writeEphemeralMCPConfig creates the file in that root scratch dir (#279), and supervision_control.go:112-126 creates .striatum/scratch/<supervisor_id> but does not write the bearer there. Concrete failing case on a faithful v3 build: daemon grants leased uid U1 --x on .striatum, --x on .striatum/scratch, rwx only on .striatum/scratch/S1; the supervised Claude lane calls current writeEphemeralMCPConfig, which stats .striatum/scratch (a real dir), then calls os.CreateTemp(<repoRoot>/.striatum/scratch, …) → because .striatum/scratch is traverse-only-not-writable, CreateTemp fails EACCES and does NOT retry under .striatum/scratch/S1. So one faithful implementation BREAKS the live Claude launch path; the other route (keep .striatum/scratch writable to preserve the current writer) violates the holder's claimed final state and leaves A22 proving a hand-planted .striatum/scratch/S1/lane-mcp-config-*.json while the real bearer is still created at .striatum/scratch/lane-mcp-config-*.json — a green test over a path the live control-plane file never uses. Either way A22 is NOT a real transition test for the file it is supposed to protect — A23 (the raw-recursive-root guard) does not catch it, since A23 only proves the group grant never targets .striatum, not that the per-leased-uid scratch ACL is compatible with the actual bearer writer. This is NOT the v2 transient group-read bug (the raw recursive grant is now forbidden); the problem is that the v3 C2 proof changes the final per-uid scratch boundary but does NOT source-anchor the necessary bearer-path migration. Required revision: move the live Claude MCP config writer to .striatum/scratch/<supervisor_id>/ (thread STRIATUM_SUPERVISOR_ID) BEFORE applying the traverse-only scratch-root ACL (or define an equivalent source-true bearer path under the lane's supervisor-scoped private dir); make A22 derive and EXERCISE the exact path writeEphemeralMCPConfig resolves (fail if any bearer is created/rewritten directly under .striatum/scratch) and assert no residual root-level .striatum/scratch/lane-mcp-config-* bearer after the transition; keep A23 (necessary but not sufficient); and make the provider/token-cache forbidden top-level set EXPLICIT enough that .gemini, .claude, .codex, and future configured provider auth paths cannot be swept into the source allowlist on re-provision. Carry-forward: NO regression in HC-A1..A5, OQ1, OQ3, OQ5, OQ6, or the narrowing invariant; no standing challenge against the raw-recursive-root prohibition itself or the credited C2 group-boundary end-state in principle. The gate-stopping problem is the source/path mismatch for the MCP bearer inside the C2 final-state and A22 proof. needs_revision."
verdict: "needs_revision"
rationale: "Cycle-2 re-adjudication of the RFC 0168 P0 v3 REVISION falsification_gate trajectory: the UNCHANGED v3 holder spec (holder/1, blob a34a7cc4) re-attacked by two SECOND-ROUND falsifiers (falsifier_1 attempt 2 = falsifier-reviewer-003; falsifier_2 attempt 2 = falsifier-reviewer-004); the cycle ends at adjudication with no further holder turn. I read the latest (attempt-2) falsifier outputs from their job refs (refs/striatum/run_.../job_..._falsifier_1/2 and …_falsifier_2/2) because the per-job worktree was still anchored at the attempt-1 run-branch HEAD; both second-round falsifiers concur with the first round in substance, and the verdict is robust to either. Gate clearing condition (SEED + adjudicator role): a clearing verdict (accept / accept_with_findings) REQUIRES BOTH C1-RESIDUAL and C2-RESIDUAL GENUINELY DISCHARGED (mechanisms anchored to real source, named tests + guards specified), no carry-forward regressed, and no standing material challenge. The v3 revision is a strong, genuine pass on ONE of its two charged residuals but not the other, so the gate does NOT clear. C1-RESIDUAL (OQ2-SCRUB-POSTCONDITION): GENUINELY DISCHARGED. I independently credit the fail-closed three-way predicate P1 — tolerate only true zombie/dead tasks (/proc state in {Z,X,x} or a PID that has fully left the re-enumerated domain) and BLOCK on every other observed state (R/S/D/T/t/I/W/P/K/…, any unrecognized char, AND an unreadable/ambiguous /proc read for a still-present PID), finalizing quarantined with a typed lane_uid_scrub_failed — wired to a NEW classifyPoolUIDTaskState(pid)->{Reaped,Live,Unknown} that is explicitly NOT the binary processZombie. I confirmed against go/pkg/supervisor/tmux_liveness.go:599-614 that processZombie reads /proc/<pid>/stat, takes the field after the last ), returns true ONLY for Z, and returns FALSE on a read error (:603-605) — exactly the binary trap an implementer would inherit; the v3 instruction to NOT wire P1 off processZombie or !processZombie is therefore load-bearing and source-correct, and the per-PID owner-uid read via /proc/<pid>/status Uid: is genuinely new (no existing supervisor reader reads /proc/<pid>/status). The stopped/traced/reparented/re-forking survivor cases are all caught (uid-domain enumeration, bounded re-kill/re-probe re-enumerating before classifying, only a fully-vanished PID tolerated). A21 TestStoppedOrTracedUIDProcessBlocksReturn names the injected stopped/traced condition, the Live classification, the quarantine+lane_uid_scrub_failed finalize, non-allocation, restart-preserved quarantine, the proof-gated clear, and the recorded /proc evidence; scrub_proof/scrub_failure record observed PIDs+states so doctor distinguishes tolerated Z residue from a non-zombie quarantine cause. Falsifier-reviewer-003 attempted the stopped/traced, reparented/detached, and re-fork-before-closure cases and found no non-zombie survivor that can still reach returned; I concur — the SEED falsifier_1 refuter (a non-zombie survivor that re-leases) does NOT exist against the v3 spec. C1-RESIDUAL DISCHARGED. C2-RESIDUAL (OQ4-ACL-PROVISIONING-TRANSITION): NOT GENUINELY DISCHARGED — a material, source-confirmed challenge (falsifier-reviewer-004, corroborating the first-round falsifier-reviewer-002) lands and stands unrebutted (no further holder turn). The PROCEDURE half of the C2 fix is genuine and credited: the allowlist / exclude-at-traversal form is mandatory, raw setfacl -R …:rX <repoRoot>-then-strip is explicitly forbidden, the OQ4 invariant adds the during clause, and the deterministic pure ACL-planner guard A23 TestACLPlannerRejectsRawRecursiveRootWhileScratchExists is the right shape — the v2 transient group-read window is closed, no challenge survives there. BUT the C2-RESIDUAL discharge ALSO requires a REAL before/during/after transition test (A22) over the actual control-plane bearer, and that test is NOT real against current source. I independently verified the source: the live MCP bearer (the Authorization: Bearer-carrying lane-mcp-config-*.json) is created by writeEphemeralMCPConfig (go/pkg/agentloop/mcpconfig.go:550) directly under <repoRoot>/.striatum/scratch (dir := filepath.Join(repoRoot,'.striatum','scratch') at :555; os.CreateTemp(dir,'lane-mcp-config-*.json') at :559; chmod 0600 at :565) — NOT under .striatum/scratch/<supervisor_id>/, and it does not thread STRIATUM_SUPERVISOR_ID. The holder's claim that mcpconfig.go writes the 0600 bearer under .striatum/scratch/<supervisor_id>/ and its source-verification CONFIRMED citation mcpconfig.go:241/266 are FALSE for the bearer: :241 is os.WriteFile of the gemini settings.json (error string 'write gemini settings'), and :266 is the gemini settings.json.backup/created marker dir — both gemini-cli settings markers, not the MCP bearer. The pty.log half of the citation IS source-true (loop.go:145 resolves .striatum/scratch/<supervisorID>/pty.log, :300 opens it 0600). The defect is verdict-driving for two reasons. (1) The v3 final scratch ACL (.striatum/scratch -> u:<leased-uid>:--x, rwx only on .striatum/scratch/<supervisor_id>) is internally inconsistent with the live writer: scratch_acl.go:31-48 grants .striatum/scratch rwx+default ACL precisely because writeEphemeralMCPConfig CreateTemps the bearer there, and the #279 comment documents that a non-owner lane 'can traverse .striatum but cannot create the file unless scratch is writable' — so a faithful build of the holder's --x-only-scratch-root final state makes os.CreateTemp(.striatum/scratch,…) fail EACCES with exactly the 'create ephemeral mcp config: permission denied' failure #279 exists to prevent, BREAKING the lane launch path. (2) The only way to keep the launch path working without moving the writer is to keep .striatum/scratch writable — which violates the holder's claimed final state AND leaves A22 asserting a hand-planted .striatum/scratch/S1/lane-mcp-config-*.json while the real bearer is still created at .striatum/scratch/lane-mcp-config-*.json (a green test over a path the live control-plane file never uses; falsifier-reviewer-004 ran the current focused agentloop tests and confirmed the surface still accepts root-scratch placement). Either way A22 is a FAKE transition test for the file it is supposed to protect — which the SEED names explicitly as a C2-RESIDUAL UNMET condition ('if falsifier_2 shows … a fake test/guard, C2-RESIDUAL is UNMET'). The holder did not state the bearer-writer migration as a required build step; it instead rests the C2 final state on a false CONFIRMED source citation, so a downstream implementer following the spec cannot land a real transition proof. The second-round falsifier additionally (and correctly) flags that the provider/token-cache forbidden top-level set must be made EXPLICIT (.gemini/.claude/.codex/credential caches) so a re-provision cannot sweep a provider auth path into the source allowlist — a genuine sharpening, though the bearer-path mismatch alone is gate-stopping. C2-RESIDUAL NOT DISCHARGED. CARRY-FORWARD: INTACT and unregressed. Both second-round falsifiers independently report NO regression in HC-A1..A5, OQ1/OQ3/OQ5/OQ6, or the narrowing invariant; I concur. The Part 1 hard core is carried verbatim from v1/v2 (re-verified vs pty.go/tmux_liveness.go HEAD); the C1 durable four-state lease machine + held-unique index + three-transaction boundary + reaper + doctor surface + quarantine-survives-restart + dirty-excluding exhaustion are carried and, with the tightened P1, more tightly closed; the C2 final .striatum/-excluding GROUP ACL end-state invariant (no g:striatum-lanes access-or-default entry under .striatum/) is INTACT and strengthened by the raw-recursive-root prohibition + A23. The C2-RESIDUAL failure is a NEW source-grounded hole in the per-supervisor scratch BEARER-PATH realness, NOT a regression of a carry-forward; the v1 OQ1 exhaustion caveat and OQ6 stale-store contingency stay closed. NEW CHALLENGES: falsifier-reviewer-003 lands nothing (C1-RESIDUAL closed); falsifier-reviewer-004 STANDS (material, source-confirmed, unrebutted). Why not accept_with_findings: the C2 hole is a no-replay / proof-realness / launch-soundness defect INSIDE the C2-RESIDUAL clearing requirement (a fake transition test over the live bearer; a final-state ACL that breaks lane launch), and the SEED's own UNMET condition for falsifier_2 (a fake test/guard) is met — this is not trackable post-clearance polish. Why not reject: C1-RESIDUAL is genuinely discharged, the C2 PROCEDURE fix (mandatory allowlist + A23 guard) is correct, no path widens admin-token exposure or mints a lane-readable shared reseal bearer, both falsifiers confirm the no-widening invariant and the proven hard core, and the remaining correction is narrow and precisely specified. Per-residual record: C1-RESIDUAL DISCHARGED; C2-RESIDUAL NOT DISCHARGED (procedure/allowlist+A23 RESOLVED; transition-test A22 + final per-supervisor scratch ACL OPEN — the live MCP bearer is at .striatum/scratch root, not the supervisor subdir, so A22 is non-real and the final ACL would break launch). Carry-forwards INTACT: HC-A1..A5 INTACT, C1 lease machine INTACT, C2 final group-ACL end-state INTACT, OQ1 INTACT (+caveat closed), OQ3 INTACT, OQ5 INTACT, OQ6 INTACT (+contingency closed), narrowing INTACT. Falsifier dispositions: falsifier_1 (reviewer-003) REBUTTED-by-spec / no material challenge; falsifier_2 (reviewer-004) STANDS. Verdict: needs_revision. Gate-cycle consequence: per the SEED/role this was the single allowed v3 revision cycle; this needs_revision EXHAUSTS the v3 gate UNCLEARED and routes the residual to the OPERATOR (do NOT ratchet into another holder cycle). The committer does NOT publish the consolidated SPEC and the operator does NOT ratify D272 on this verdict (D271 remains reserved by the concurrent RFC 0170 P0 design). The remaining fix is small and source-anchored: (C2) move the live MCP bearer writer writeEphemeralMCPConfig from <repoRoot>/.striatum/scratch to <repoRoot>/.striatum/scratch/<supervisor_id>/ (threading STRIATUM_SUPERVISOR_ID) BEFORE re-keying the scratch ACLs; make A22 derive and exercise the exact path writeEphemeralMCPConfig resolves (not a hand-planted fixture) and assert no residual root-level .striatum/scratch/lane-mcp-config-* bearer after the transition; make the provider/token-cache forbidden top-level set explicit (.gemini/.claude/.codex/credential caches); and correct the source citation (the bearer is mcpconfig.go:550-559, not :241/266). The maintainer-ratified direction (per-lane pooled OS uid, D261) carries regardless; adjudicator clearance gates the spec's soundness, not the product call."
findings:
  - id: HC-ORACLE-INTACT
    severity: high
    posture: design
    status: accepted
    source_refs: ["dialogue:1", "dialogue:2", "dialogue:3"]
    affected_invariants:
      - "structural no-replay: a per-lane uid dissolves the BC1-W1-ORACLE same-uid tmux/0600/ptrace replay class on this host (Yama ptrace_scope=1) — CARRIED FORWARD UNREGRESSED from v1/v2"
    challenge: "INTACT (carry-forward). The v1-proven hard core HC-A1..A5 (per-uid tmux 0700 socket so a sibling cannot respawn-pane the target pane; cross-uid 0600 DAC; cross-uid ptrace/setns//proc denial by ptrace_may_access — the exact axis namespace-inode failed under D261; SO_PEERCRED uid discriminator; every residual same-uid surface closed) is carried into v3 verbatim (HOLDER Part 1, re-verified vs pty.go/tmux_liveness.go HEAD). Both second-round falsifiers independently re-checked and found NO regression. The two residual fixes only remove surface; they do not touch the launch path or the structural claim. Not reopened, not regressed."
  - id: OQ2-SCRUB-POSTCONDITION
    severity: high
    posture: design
    status: accepted
    source_refs: ["dialogue:1", "dialogue:2"]
    affected_invariants:
      - "no cross-lease same-uid residue: a returned uid must be PROVABLY empty before re-lease — the scrub postcondition must prove zero non-zombie uid-owned tasks, not merely zero R/S/D tasks (the v2 residual hole)"
    challenge: "DISCHARGED — C1-RESIDUAL genuinely closed (falsifier-reviewer-003, second round, lands no material challenge; corroborates first-round falsifier-reviewer-001). v2 P1 blocked return only on R/S/D and tolerated Z, leaving non-zombie T (stopped) / t (tracing-stop) survivors — which still hold the uid/memory/FDs/HOME-credential reachability and are SIGCONT-resumable — as a dirty re-lease path. v3 restates P1 as a fail-closed three-way classification: tolerate only true zombie/dead tasks (/proc state in {Z,X,x} or a PID fully gone from the re-enumerated domain) and BLOCK on every other observed state (R/S/D/T/t/I/W/P/K/…, any unrecognized char, AND an unreadable/ambiguous /proc read for a still-present PID), finalizing quarantined with a typed lane_uid_scrub_failed (HOLDER.md:214-235,:293-318). It is wired to a NEW classifyPoolUIDTaskState(pid)->{Reaped,Live,Unknown}, explicitly NOT the binary processZombie, with a spec note + A21 preventing a processZombie / !processZombie wiring. I independently confirmed processZombie is binary Z-or-not and returns FALSE on a read error at go/pkg/supervisor/tmux_liveness.go:599-614 (state parse :607-613; read-error->false :603-605) — so the trap is real and the v3 instruction to avoid it is load-bearing; the per-PID owner-uid enumeration via /proc/<pid>/status Uid: is genuinely new (no existing supervisor reads /proc/<pid>/status). The stopped/traced/reparented/re-forking survivor cases are all blocked (uid-domain enumeration, bounded re-kill/re-probe re-enumerates before classifying, only a vanished PID tolerated). A21 TestStoppedOrTracedUIDProcessBlocksReturn is concrete (Live classification, quarantine+lane_uid_scrub_failed, non-allocation, restart-preserved quarantine, proof-gated clear, recorded /proc evidence); scrub_proof/scrub_failure record observed PIDs+states so doctor distinguishes tolerated Z residue from a quarantine cause. No C1 carry-forward regressed. C1-RESIDUAL DISCHARGED."
  - id: OQ4-ACL-PROVISIONING-TRANSITION
    severity: high
    posture: design
    status: open
    source_refs: ["dialogue:1", "dialogue:3"]
    affected_invariants:
      - "ACL exactly-enough without over-grant: the .striatum/ control-plane (MCP bearer, PTY log, token cache, foreign worktree) must be unreadable by a non-leased OR different-leased pool uid before/during/after provisioning, AND the C2-RESIDUAL transition test must exercise the REAL live-source bearer path, AND the final per-uid scratch ACL must not break the lane launch path"
    challenge: "OPEN — verdict-driving (falsifier-reviewer-004, second round; corroborates first-round falsifier-reviewer-002 and strengthens it with the live test surface). C2-RESIDUAL NOT genuinely discharged. The PROCEDURE half of the C2 fix is genuine and CREDITED: the allowlist / exclude-at-traversal form is made mandatory, raw setfacl -R …:rX <repoRoot>-then-strip is explicitly forbidden, the OQ4 invariant gains the during clause, and the deterministic pure ACL-planner guard A23 TestACLPlannerRejectsRawRecursiveRootWhileScratchExists is the right shape — the v2 transient group-read window is closed, no challenge survives there. BUT the C2-RESIDUAL discharge also requires a REAL before/during/after transition test (A22) over the actual control-plane bearer, and it is NOT real against current source. The holder claims (and marks CONFIRMED, citing mcpconfig.go:241/266) that the 0600 MCP bearer lives under .striatum/scratch/<supervisor_id>/; that is source-FALSE. I verified the live bearer is created by writeEphemeralMCPConfig directly under <repoRoot>/.striatum/scratch (go/pkg/agentloop/mcpconfig.go:550 dir := filepath.Join(repoRoot,'.striatum','scratch') :555; os.CreateTemp(dir,'lane-mcp-config-*.json') :559; chmod 0600 :565) — NOT the supervisor subdir, and it does not thread STRIATUM_SUPERVISOR_ID. The cited :241 is os.WriteFile of the gemini settings.json ('write gemini settings'), :266 is the gemini settings.json.backup/created marker dir — the gemini-cli markers, not the bearer; only the pty.log half (loop.go:145,:300) is source-true. The current test surface still encodes the root-scratch shape (mcpconfig_test.go:107-128 asserts only the .striatum/scratch prefix; :163-170 CreateTemps under .striatum/scratch), which the second-round falsifier confirmed by running the focused agentloop tests. Two verdict-driving consequences: (1) the v3 final scratch ACL (.striatum/scratch -> u:<leased-uid>:--x, rwx only on .striatum/scratch/<supervisor_id>) is internally inconsistent with the live writer — scratch_acl.go:31-48 grants rwx+default on .striatum/scratch ITSELF precisely because writeEphemeralMCPConfig CreateTemps there, and the #279 comment documents that a non-owner lane cannot create its ephemeral MCP config under .striatum/scratch without a writable-scratch ACL, so a faithful build of the holder's --x-only-scratch-root final state makes os.CreateTemp fail EACCES with the exact 'create ephemeral mcp config: permission denied' error #279 prevents, BREAKING lane launch; (2) keeping .striatum/scratch writable to preserve launch violates the claimed final state and leaves A22 asserting a hand-planted .striatum/scratch/S1/lane-mcp-config-*.json while the real bearer is created at .striatum/scratch/lane-mcp-config-*.json — a green test over a path the live bearer never uses. Either way A22 is a FAKE transition test for the file it protects — the SEED's named C2-RESIDUAL UNMET condition ('if falsifier_2 shows … a fake test/guard, C2-RESIDUAL is UNMET'). A23 does not catch it (it only proves the group grant never targets .striatum, not that the per-uid scratch ACL is compatible with the bearer writer). The holder did not state the bearer-writer migration as a required build step; it rests the final state on the false CONFIRMED citation. needs_revision, not accept_with_findings (a fake transition proof + a final-state ACL that breaks lane launch are soundness defects inside the C2-RESIDUAL gate). Required residual fix (routed to the operator): move writeEphemeralMCPConfig from <repoRoot>/.striatum/scratch to <repoRoot>/.striatum/scratch/<supervisor_id>/ (thread STRIATUM_SUPERVISOR_ID) BEFORE re-keying the scratch ACLs; make A22 derive and exercise the exact path writeEphemeralMCPConfig resolves (not a hand-planted fixture) and assert no residual root-level .striatum/scratch/lane-mcp-config-* bearer after the transition; keep A23 (necessary, not sufficient); make the provider/token-cache forbidden top-level set explicit (.gemini/.claude/.codex/credential caches); correct the source citation (bearer is mcpconfig.go:550-559, not :241/266)."
  - id: OQ-CARRY-FORWARD-INTACT
    severity: high
    posture: design
    status: accepted
    source_refs: ["dialogue:1", "dialogue:2", "dialogue:3"]
    affected_invariants:
      - "the v1+v2-credited carried set into v3 unregressed: the C1 durable four-state lease machine, the C2 final .striatum/-excluding GROUP ACL end-state invariant, OQ1 sizing+fail-closed exhaustion, OQ3 launch-as-only provisioning, OQ5 generation token, OQ6 hydration shape, restart-survival, and the narrowing invariant"
    challenge: "INTACT (carry-forward, no regression from either predicate tightening). Both second-round falsifiers report NO regression and I concur. C1 lease machine: four-state active/scrubbing/quarantined/returned, partial held-unique index, three-transaction allocate/scrub-begin/scrub-finalize boundary (scrub side effects out of tx), failed-proof-quarantines, leaked-active + stuck-scrubbing reaper, quarantine-survives-restart, dirty-excluding exhaustion (free=N−active−scrubbing−quarantined) — all carried, and MORE tightly closed by the tightened P1 (a stopped/traced survivor now quarantines instead of returning dirty). C2 final GROUP ACL end-state: the hard .striatum/ boundary invariant (no g:striatum-lanes access-or-default entry under .striatum/, .git/, or token-cache, before/during/after) is INTACT and strengthened by the mandatory allowlist + A23 planner guard; the C2-RESIDUAL failure is a NEW source-grounded hole in the per-supervisor scratch BEARER-PATH realness, not a regression of the group-boundary end-state. OQ1: host-global ceiling + typed fail-closed lane_uid_pool_exhausted carried, v1 caveat stays closed (A20). OQ3: static host-runbook pool, daemon holds only the launch-as (%striatum-lanes) grant, scrub uses only that grant — no useradd/userdel (A12). OQ5: leased-uid + monotonic per-uid generation token minted in tx_alloc, compared on every attestation AND control-frame path (A14). OQ6: per-spawn per-uid hydration into the leased uid's 0600 store scrubbed on return, contingency closed by P3 (a failed P3 quarantines; A15). Restart-survival: free set DERIVED from PostgreSQL, never memory; quarantine survives a boot-epoch rotation (A8'/A11'/A19). NARROWING: both residual fixes only remove surface (C1 adds quarantine causes; C2 removes a transient exposure); no new authority. Do NOT reopen or regress."
branches:
  design: blocked
---

# Collaboration Ledger — RFC 0168 P0 design (v3 REVISION, cycle 2)

author: adjudicator-author-002

> Cycle-2 re-adjudication of the RFC 0168 P0 **v3 REVISION** `falsification_gate`
> dialogue trajectory (*per-lane OS uid as the lane security principal*). The v3
> Holder spec is **unchanged** from cycle 1 (`holder/1`, blob `a34a7cc4`); this
> round re-adjudicates it against the **second-round falsifiers** —
> `falsifier_1` attempt 2 (`falsifier-reviewer-003`) and `falsifier_2` attempt 2
> (`falsifier-reviewer-004`). The revision was charged to **discharge exactly the
> two source-anchored residual holes** the v2 cycle-1 ledger left open and routed
> to the operator — **C1-RESIDUAL** (`OQ2-SCRUB-POSTCONDITION`: the
> scrub-postcondition process predicate P1) and **C2-RESIDUAL**
> (`OQ4-ACL-PROVISIONING-TRANSITION`: the ACL provisioning transition) — while
> carrying the v1-proven hard core (`HC-A1..A5`), the v2-credited C1 durable
> four-state lease machine and C2 final `.striatum/`-excluding GROUP-ACL end-state,
> and `OQ1/OQ3/OQ5/OQ6` + the narrowing invariant forward **unregressed**. The
> direction (a pre-provisioned pool of per-lane uids, leased per lane) is
> maintainer-ratified (**D261**, 2026-06-24) and is **not** relitigated. Inputs
> read: the revised Holder spec (`dialogue/holder/HOLDER.md`), the **second-round**
> falsifier re-attacks (read from their job refs `…job_…_falsifier_1/2` and
> `…_falsifier_2/2`, since the per-job worktree was still anchored at the
> attempt-1 run-branch HEAD), the v3 `SEED.md` charter + adjudicator role, RFC
> 0168, and — as required context for what the revision had to fix — the banked v2
> SPEC (`context/v2_HOLDER.md`), the v2 cycle-1 ledger
> (`context/v2_COLLABORATION_LEDGER_cycle_1.md`, its `OQ2-SCRUB-POSTCONDITION` +
> `OQ4-ACL-PROVISIONING-TRANSITION` findings), and the v2 falsifiers. No raw
> terminal output or private diagnostics were read as evidence. **Every
> load-bearing source citation below was independently re-verified against the
> current worktree HEAD.**

## Verdict

**verdict: needs_revision**

The v3 revision **genuinely discharges one of its two charged residuals but not
the other**, so the gate does **not** clear. A clearing verdict
(`accept` / `accept_with_findings`) requires **both** C1-RESIDUAL **and**
C2-RESIDUAL genuinely discharged with their named tests/guards and no standing
material challenge.

- **C1-RESIDUAL (`OQ2-SCRUB-POSTCONDITION`) — DISCHARGED.** P1 is correctly
  tightened to a **fail-closed three-way** predicate (tolerate only true
  zombie/dead `{Z,X,x}` or a fully-vanished PID; **block** every non-zombie state
  incl. **`T`/`t`**, any unknown char, **and** an unreadable still-present PID),
  wired to a **new** `classifyPoolUIDTaskState` that is **not** the binary
  `processZombie` (`tmux_liveness.go:599-614`, read-error→`false`), with `/proc`
  evidence recorded and the A21 negative test. I confirmed `processZombie` against
  live source; the second-round Falsifier 1 (`reviewer-003`) lands nothing and I
  concur — no non-zombie `pool_uid` survivor can reach `returned`.
- **C2-RESIDUAL (`OQ4-ACL-PROVISIONING-TRANSITION`) — NOT DISCHARGED.** The
  *procedure* fix is genuine and credited (mandatory allowlist, raw-recursive-root
  forbidden, the A23 planner guard, the v2 transient window closed), yet the
  second-round Falsifier 2 (`reviewer-004`) lands the same **material,
  source-confirmed** challenge as the first round — and strengthens it: the
  revised C2 final state and the A22 transition test assume the MCP bearer lives
  under `.striatum/scratch/<supervisor_id>/`, but the live
  `writeEphemeralMCPConfig` (`mcpconfig.go:550-559`) writes it under
  `.striatum/scratch/` **root** (the holder's `CONFIRMED` `mcpconfig.go:241/266`
  citation actually points at the gemini-cli settings markers). So **A22 is a fake
  transition test** for the file it protects, and the `--x`-only-scratch-root final
  ACL **breaks the lane launch path** (`scratch_acl.go:31-48` / #279:
  `os.CreateTemp(.striatum/scratch,…)` → `EACCES`). The falsifier also correctly
  asks for the provider/token-cache forbidden set (`.gemini`/`.claude`/`.codex`) to
  be made explicit.

No carry-forward regressed (HC-A1..A5, the C1 lease machine, the C2 GROUP-ACL
end-state invariant, OQ1/OQ3/OQ5/OQ6, the narrowing invariant all **INTACT**). No
admin-token widening, no lane-readable shared reseal bearer, no elevated
credential — `needs_revision`, **not** `reject`; the fake C2 transition proof + a
launch-breaking final ACL are soundness defects **inside** the C2-RESIDUAL gate,
not trackable post-clearance findings — so **not** `accept_with_findings`.

## Gate-cycle consequence

This was the **single allowed v3 revision cycle**. A `needs_revision` here
**exhausts the gate uncleared** and routes the residual to the **operator** — do
**not** ratchet into another holder cycle. The committer does **not** publish the
consolidated SPEC, and the operator does **not** ratify **D272** on this verdict
(**D271** remains reserved by the concurrent RFC 0170 P0 design). The remaining
correction is small and source-anchored: **(C2)** move the live MCP bearer writer
`writeEphemeralMCPConfig` from `<repoRoot>/.striatum/scratch` to
`<repoRoot>/.striatum/scratch/<supervisor_id>/` (threading `STRIATUM_SUPERVISOR_ID`)
**before** re-keying the scratch ACLs; make **A22** derive and exercise the exact
path the live writer resolves (failing if any bearer is created/rewritten directly
under `.striatum/scratch`) and assert no residual root-level
`.striatum/scratch/lane-mcp-config-*` bearer after the transition; keep **A23**
(necessary, not sufficient); make the provider/token-cache forbidden top-level set
explicit; and correct the source citation (the bearer is `mcpconfig.go:550-559`,
not `:241/266`). The maintainer-ratified direction (per-lane pooled OS uid, D261)
carries regardless — adjudicator clearance gates the spec's **soundness**, not the
product call.

<sub>Adjudicator collaboration ledger — RFC 0168 P0 `falsification_gate` design run,
**REVISION v3, cycle 2** (re-adjudication of the unchanged v3 Holder against the
second-round falsifiers `falsifier-reviewer-003` / `falsifier-reviewer-004`).
**C1-RESIDUAL DISCHARGED** (the fail-closed three-way scrub-postcondition predicate
P1 — every non-zombie `pool_uid`-owned task `T`/`t`/unknown/unreadable-still-present
**blocks** `returned` and finalizes `quarantined` — wired to the new
`classifyPoolUIDTaskState`, not the binary `processZombie` `tmux_liveness.go:599-614`;
`/proc` evidence recorded; A21). **C2-RESIDUAL NOT DISCHARGED**: the *procedure* fix
(mandatory allowlist, raw-recursive-root forbidden, A23) is credited, but the C2
final-state + A22 transition test are **not source-true** for the live MCP bearer —
`writeEphemeralMCPConfig` (`mcpconfig.go:550-559`) writes it under `.striatum/scratch/`
root, so A22 is a **fake** transition test and the `--x`-only-scratch-root final ACL
**breaks lane launch** (#279). Carry-forwards INTACT (HC-A1..A5, the C1 lease machine,
the C2 GROUP-ACL end-state invariant, OQ1/OQ3/OQ5/OQ6, narrowing). `needs_revision` —
single v3 cycle exhausted; the C2 residual (move the bearer writer to the supervisor
scratch dir; make A22 exercise the real resolved path; make the provider forbidden set
explicit; fix the citation) routes to the operator. D261 carries regardless.</sub>
