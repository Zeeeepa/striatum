Read the workflow packet and all required context docs before editing.

Implement the RFC 0168 P0 build inside the declared write scope. Treat D272's
follow-up as binding: provider-owned credential selectors must fail closed when
uncovered and resolving in-repo, while ordinary non-credential lane env such as
AGY_HOME or FIXTURE_CONFIG_DIR must still launch.

Publish the required DRAFT.md with a concise implementation ledger: files
changed, design assertions/gates discharged, tests run, and residuals for the
reviewer/operator.
