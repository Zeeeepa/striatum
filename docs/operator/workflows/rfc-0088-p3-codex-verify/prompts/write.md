# Verify task: codex agent_loop PTY lane

You are running as a daemon-owned interactive agent-loop lane. Do exactly this, then stop:
1. Write docs/operator/workflows/rfc-0088-p3-codex-verify/artifacts/VERIFY.md with the title block plus one line: `RFC 0088 P3 codex agent_loop PTY lane verified end-to-end.` plus your author byline line.
2. Publish it as the expected artifact (logical_name verify_note, kind handoff) using your MCP tools / packet commands.
3. Complete the job.
Do not modify anything else. Do not ask questions.
