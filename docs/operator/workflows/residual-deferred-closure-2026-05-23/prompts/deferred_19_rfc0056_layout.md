# Deferred 19 RFC 0056 Layout Follow-Up

Classify workflow-file generation and artifact-root `.gitignore` policy for
consumer layout scaffolds. Preserve current product boundary and avoid
surprising target-repo writes.
