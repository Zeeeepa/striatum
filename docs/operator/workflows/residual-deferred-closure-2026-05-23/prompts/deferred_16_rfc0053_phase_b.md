# Deferred 16 RFC 0053 Phase B

Classify the `human_checkpoint` to `escalation_checkpoint` and `waiting_human`
rename. Treat workflow schema bump and upgrade path as required before any
breaking rename.
