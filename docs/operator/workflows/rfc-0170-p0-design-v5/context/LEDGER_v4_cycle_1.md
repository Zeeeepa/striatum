---
schema_version: "striatum.collaboration_ledger.v1.1"
artifact_kind: "collaboration_ledger"
shape: "falsification_gate"
author: adjudicator-author-001
workflow: "rfc-0170-p0-design-v4"
run_id: "run_962da512bc23aaa09c84f1007107c76d"
cycle: 1
topic: "RFC 0170 P0 falsifiable implementation SPEC (self-culling repository: the Tier-1 cullable_entity candidacy substrate + read-only DecayTickSweep) — v4 REVISION gate scoped to exactly two residuals: G1''' (make clause 3's protected pathspec fully static + tree-local so the corpus nominates exactly {D267, D081}) and G2''' (close the cursor-refresh-timing carrier in the HANG control), without regressing the credited latency machinery, the decision-successor rule, the clause-4 active-baseline citation rule, or the cleared G3/G4"
participants:
  - holder
  - falsifier_1
  - falsifier_2
  - adjudicator
entries:
  - kind: claim
    by: holder
    refs: ["dialogue:1"]
    text: "v4 revising-holder SPEC. Carries INTACT from v3 (the SEED's do-not-reopen set): §2 substrate (runtime migration 0045_cullable_entity.sql under go/pkg/db/sql/, both read+write authority-inventory rows, striatumd_rw GRANT with no owner DDL/FK for >=27, no SELECT *), the clause-4 active-baseline inbound-citation rule (4a-4f) preserving rfc:0097/0027/0039/0041 (and D174 via live RFC 0109), the DISCHARGED kind=decision successor-extraction rule (own-row cell 3 then cell 5 by precedence, the \\bsupersed(?:ed|es) by\\s+<reflist> regex, sentence boundary, multi-ref split; D267->{D270}, D081->{D087,D094,D104}, bare state-cell-only `superseded` withheld), the entire latency machinery (DefaultCullFoldTimeout=10s over both the DB read and the filesystem scan, deadline-bounded child goroutine, single-in-flight skip-on-overrun, L4 compute-then-commit), §5 OQ1, and §6 P0/P1+ boundary. Changes EXACTLY two things. G1''' discharge attempt: §3 clause 3 drops the v3 leftover dynamic 'any path referenced by an open GitHub issue' bullet so the cull-specific protected pathspec is a fully static, tree-local checked-in manifest with no external-state term (issue-linked preservation kept only as an operator advisory OUTSIDE cullable_entity); the manifest protects docs/records/_frozen/**, the design-scaffold/fixture roots, and the named root files, while leaving docs/rfcs/ and docs/decisions/ eligible; the corpus is re-stated to nominate exactly {decision:D267, decision:D081}. G2''' discharge attempt: §4 takes the SEED's preferred option (b) — the cull fold is moved OFF the scheduler's wait-gating path (it returns in O(1) after innerSweep, launching/skipping a detached single-in-flight scan goroutine the recovery loop never joins), so a hung fold cannot postpone the next scheduler_cursors refresh by construction; B5 keeps the carried Sweeps==2 / no-torn-write value control and ADDS a refresh-not-deferred assertion (tick-2 cursor refresh at the same fake-clock instant under the hung-cull variant A and the no-cull control B), which FAILS if the fold is wired back onto the wait-gating path. Holder asserts the credited deadline + watchdog + skip-on-overrun + L4 compute-then-commit machinery is unchanged in substance and that the detached scan self-terminates within DefaultCullFoldTimeout over both halves."
  - kind: challenge
    by: falsifier_1
    refs: ["dialogue:2"]
    correspondence: landed_unrebutted
    text: "G1''' does NOT fully discharge, though the named v3 defect IS closed. CREDITED: the v4 pathspec contains no open-issue/GitHub/API/external-state term (HOLDER.md:376-425); docs/rfcs/*.md and docs/decisions/decision-log.md are no longer protected by clause 3; D267 still derives cleanly (decision-log.md:38 state `superseded`, cell 3 `SUPERSEDED by D270`, D270 implemented/live at decision-log.md:35, only non-protected hit is D270's 4e-excluded backref); the preserved RFC set rfc:0097/0027/0039/0041 and D174 are untouched. STANDING defect (new, in-tree, not a replay): the static manifest protects only docs/records/_frozen/** but the live tree carries tracked status:frozen records OUTSIDE that root under docs/records/audits/**, which the manifest does NOT protect. docs/records/audits/STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md is tracked, front-matter `status: frozen`, and is a front-matter-carrying docs/** artifact in the clause-4b inbound-scan set. Under clause 4c a source is live iff its structural status does not begin superseded/tombstoned/withdrawn/deprecated AND it is not under a protected root: `frozen` begins none of the four, and audits/ is not in the pathspec, so the file is a LIVE source. It carries canonical D081 hits outside D081's own row at :323 (`D081 -> D087/D094/D104`) and :367-370 (a rejected-then-validated dangling-successor discussion) whose matched physical lines contain no 4f closure-lexicon token (and :97 likewise), so they are counted active-baseline citations (not own row 4d, not a named successor 4e, not a reference-link def / README index row 4f). >=1 counted hit makes clause 4 true for D081, sets reachable_from_root=true, and WITHHOLDS it, so the corpus nominates at most {D267}, not {D267, D081} — the holder's corpus claim (HOLDER.md:481-486: D081's only inbound hit is its own row) is false, and the true-positive set is non-derivable / a two-implementer split (literal manifest -> count the hit -> {D267}; 'frozen records are provenance' -> exclude -> {D267,D081}, but that exclusion is not encoded). Fix: add docs/records/** (or a status:frozen exclusion / docs/records/audits/**) to the static protected/frozen source set, OR define status:frozen as non-live for clause 4c, then re-run the D081 inbound corpus with a table-driven case for the audit file. Verdict: needs_revision."
  - kind: challenge
    by: falsifier_2
    refs: ["dialogue:3"]
    correspondence: landed_unrebutted
    text: "The G2''' cursor-refresh-timing carrier IS closed (credited as written): source order supports option (b) — RunScheduler waits only after SweepOnce returns (scheduler.go:55-80) and ActiveRunSweep.SweepOnce refreshes the persisted recovery cursor inside innerSweep (sweep.go:114,246-263) BEFORE any post-innerSweep fold, so if the cull fold does only O(1) spawn/skip work and the recovery loop never joins the detached scan, a hung scan cannot delay the next scheduler_cursors refresh and the v3 same-wall-clock doctor carrier is removed; B5's refresh-not-deferred assertion is the right shape and binds (fails with the fold back on the wait-gating path). STANDING defect (no-regression of the credited latency machinery): the holder also claims cullCtx 'bounds both halves' and the detached goroutine self-terminates within DefaultCullFoldTimeout over the filesystem scan (HOLDER.md:640-650,657-675,820-825) — but the off-path move fixed scheduler/doctor TIMING, it did NOT make the scan preemptible. The SPEC contradicts itself: HOLDER.md:657-659 admits `context.WithTimeout` alone does not abort a blocking syscall already in flight, yet L3 then claims 'even a scan stuck in a blocking syscall is abandoned by its own goroutine' and B5 assertion 6 requires that same blocked goroutine to observe cullCtx.Done() and terminate at the timeout. A fake scan that selects on cullCtx.Done() proves only a COOPERATIVE hang, not the non-cooperative filesystem carrier the holder itself names; a scan that truly never returns cannot satisfy assertion 6. So a non-cooperative filesystem block can (i) permanently hold the single in-flight cull slot, making every later tick skip the cull forever (silent cull-liveness failure until daemon restart), or (ii) if the slot is released on timeout, admit a stale late writer violating L4 compute-then-commit unless an unspecified generation/epoch fence is added. The recovery-scheduler/doctor SAFETY rebuttal is real (detached fold never blocks RunScheduler, never defers upsertSchedulerCursor, never writes doctor inputs), but it does not discharge the no-regression requirement on the 'deadline over the filesystem scan' property the holder continues to claim and B5 does not test. Fix: either declare and bound the cull-liveness degradation explicitly (a non-cooperative FS hang disables the cull fold until restart; safety preserved) with a visible/logged test that it cannot write after timeout, OR add a stale-worker/generation fence (timeout releases the slot, late results rejected before write) proven by a non-cooperative-hang test, OR constrain the scan to interruptible operations and prove it with a negative control that does not merely cooperate with ctx.Done()."
  - kind: nomination
    by: falsifier_1
    refs: ["dialogue:2"]
    text: "Credited (G1 progress + no-regression, recorded, not gate-clearing): the v3 binding open-issue/external-state defect is fully closed — the pathspec is now a static checked-in manifest with no GitHub/API/open-issue term, docs/rfcs/ and docs/decisions/ are eligible (withheld only by clauses 1/2/4), D267 derives cleanly end-to-end, and the preserved RFC set rfc:0097/0027/0039/0041 plus D174 (live RFC 0109 path) is untouched. The kind=decision successor-extraction rule and the clause-4 active-baseline citation rule are un-regressed. The single standing defect is the static manifest's failure to protect tracked status:frozen records that live outside docs/records/_frozen/ (the docs/records/audits/** surface), which newly exposes them as live clause-4 sources now that clause 3 no longer blanket-protects the candidacy roots."
  - kind: nomination
    by: falsifier_2
    refs: ["dialogue:3"]
    text: "Credited (G2''' carrier discharged + safety/no-regression of the doctor property, recorded): option (b) is source-true — the off-the-wait-path fold removes the v3 refresh-timing carrier by construction (RunScheduler waits only after SweepOnce returns; innerSweep refreshes the cursor before the fold; doctor reads the persisted cursor JSON, doctor.go:383-390), so a hung fold cannot make doctorRecoverySweepCursor read a staler cursor than the no-cull control; recovery-result preservation, no torn write, Sweeps==2, and the same-instant refresh-not-deferred timing all hold, with the refresh-not-deferred assertion failing if the fold is put back on the wait-gating path. G3 substrate and G4 forward-compat remain un-regressed (0045 runtime migration, both inventory rows, explicit-column reads / no SELECT *, no smuggled P1+ tombstone/deletion/page/doctor-class/run-admission action). The standing defect is confined to the over-claimed 'deadline over the non-cooperative filesystem scan / detached-goroutine self-termination' property and the single-slot cull-liveness consequence, not the cursor-refresh repair or a substrate/forward-compat regression."
  - kind: constraint
    by: adjudicator
    refs: ["dialogue:2"]
    text: "G1''' (residual, routes to v5): the open-issue/external-state defect IS discharged (the pathspec is now fully static and tree-local with no external term; docs/rfcs/ and docs/decisions/ are eligible; D267 derives; the preserved RFC set + D174 is untouched). The residual is a NEW corpus-derivability defect: the static protected/frozen manifest protects only docs/records/_frozen/** but the live tree carries >=9 tracked status:frozen records OUTSIDE that root under docs/records/ (docs/records/INDEX.md plus 8 docs/records/audits/*.md, including STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md). With clause 3 no longer blanket-protecting the roots, those frozen records are now LIVE clause-4 sources, and the named audit's counted (4f-surviving) D081 hits at :97/:323/:367-370 withhold D081, so the corpus nominates at most {D267}, not {D267, D081} — the published true-positive set is non-derivable and an exposed frozen artifact survives. Make the protected/frozen source set cover ALL tracked status:frozen provenance (e.g. extend the manifest to docs/records/** or add a tree-local status:frozen-record exclusion to clause 4c), keeping docs/rfcs/ and docs/decisions/ eligible; then re-derive the G1 corpus with a protected-source fixture and a table-driven case for the audit file proving D081 is again nominated with ZERO counted inbound hits and the preserved set untouched, so two implementers read every row identically."
  - kind: constraint
    by: adjudicator
    refs: ["dialogue:3"]
    text: "G2''' (residual, routes to v5): the cursor-refresh-timing carrier IS discharged — option (b) (fold off the wait-gating path) removes it by construction, the source order confirms it, and the B5 refresh-not-deferred assertion binds (fails with the fold on the wait-gating path). Keep that repair. The residual is the no-regression of the credited latency machinery's 'deadline over the filesystem scan' half: the v4 detached rewrite over-claims that the scan self-terminates within DefaultCullFoldTimeout while HOLDER.md:657-659 admits context.WithTimeout cannot abort an in-flight blocking syscall, and B5 assertion 6 can only model a cooperative hang, leaving a non-cooperative filesystem hang to either permanently occupy the single cull slot (cull-liveness failure until restart) or admit a stale late writer. Discharge with ONE of: (i) declare and bound the cull-liveness degradation explicitly as a doctor/operability decision (a non-cooperative FS hang disables the cull fold until restart; recovery/doctor safety preserved) with a test that the stuck worker is visible/logged and cannot write after timeout; OR (ii) add a stale-worker/generation fence — the timeout releases the cull slot for future ticks and any late result from the old worker is rejected before the L4 write — proven by a non-cooperative-hang test that future cull ticks still run without stacking writers or torn cullable_entity writes; OR (iii) constrain the scan to interruptible operations and prove it with a negative control that does not merely cooperate with ctx.Done(). Keep the credited deadline/watchdog/skip-on-overrun/L4 compute-then-commit machinery and the off-path cursor-refresh repair unchanged."
verdict: "needs_revision"
rationale: "needs_revision, and because this is v4's single proper revision round, the gate is EXHAUSTED: the residuals route to the OPERATOR for a fresh -v5 — do not ratchet, do not let the committer publish the consolidated SPEC, do not ratify D271. This is NOT a clearing verdict because G1''' is not fully discharged (and a no-regression defect in the credited latency machinery also stands), even though BOTH v4 changes made real, creditable progress and the credited/cleared properties are otherwise confirmed un-regressed. G1''' UNMET: Falsifier 1 STANDS. The v3 binding defect IS closed — the clause-3 pathspec is now fully static and tree-local with no open-issue/GitHub/external-state term (HOLDER.md:376-425), docs/rfcs/ and docs/decisions/ are eligible, D267 derives cleanly (decision-log.md:38 cell 2 `superseded`, cell 3 `SUPERSEDED by D270`, D270 implemented/live at decision-log.md:35, sole non-protected hit is D270's 4e-excluded backref), and rfc:0097/0027/0039/0041 + D174 are untouched. But G1''' requires the corpus to nominate EXACTLY {D267, D081} with no exposed frozen artifact, and it does not: the static manifest protects only docs/records/_frozen/** while the live tree carries tracked status:frozen records OUTSIDE that root under docs/records/ (verified: docs/records/INDEX.md + 8 docs/records/audits/*.md). docs/records/audits/STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md is tracked with front-matter `status: frozen` (verified) — not under a protected root and not status-dead (`frozen` begins none of superseded/tombstoned/withdrawn/deprecated), so it is a LIVE clause-4 source. Its D081 hits at :323 (`D081 -> D087/D094/D104`) and :367-370 (and :97) carry NO 4f closure-lexicon token on the matched physical line (verified by grep over the fixed lexicon), and are not own-row (4d), not a named-successor row (4e), not a reference-link def / README index row (4f) — so they are counted active-baseline citations. >=1 counted hit sets reachable_from_root=true and WITHHOLDS D081, so the corpus nominates at most {D267}, not {D267, D081}, and the holder's claim (HOLDER.md:481-486: D081's only inbound hit is its own row) is source-false. This is exactly the gate's 'exposed frozen artifact' + 'corpus does not nominate exactly {D267, D081}' G1''' UNMET condition, and a two-implementer split internal to the SPEC. It is a NEW in-tree corpus defect (newly reachable once clause 3 stopped blanket-protecting the candidacy roots), not a replay of the open-issue pathspec defect. G2''' carrier DISCHARGED, but a no-regression defect STANDS: Falsifier 2 STANDS on the no-regression sub-point. The cursor-refresh-timing carrier itself is genuinely closed by option (b): RunScheduler calls wait(interval) only AFTER SweepOnce returns (scheduler.go:55-80, verified), innerSweep refreshes the persisted recovery cursor (sweep.go:114,246-263, verified) BEFORE the post-innerSweep fold, and doctor reads that persisted cursor JSON (doctor.go:383-390, verified) — so a fold that returns in O(1) off the wait-gating path provably cannot defer the next refresh, and the B5 refresh-not-deferred assertion binds and fails with the fold back on the wait-gating path. That part of G2''' is MET and credited. But the holder ALSO re-asserts the credited latency machinery is unchanged, including a 'deadline over the filesystem scan' that makes the detached goroutine self-terminate within DefaultCullFoldTimeout — and the SPEC contradicts itself (HOLDER.md:657-659 admits context.WithTimeout cannot abort an in-flight blocking syscall, while L3/B5 assertion 6 claim the blocked goroutine observes cullCtx.Done() and terminates at the timeout). A non-cooperative filesystem hang therefore either permanently holds the single in-flight cull slot (every later tick skips the cull forever — an undeclared cull-liveness failure until restart) or, if the slot is released on timeout, admits a stale late writer against L4 unless an unspecified generation fence is added; B5 as written can only model a cooperative hang and so does not test the named carrier. This is a real no-regression gap on a credited property and independently warrants revision, even though recovery/doctor SAFETY (loop never blocked, no torn write, refresh not deferred) is preserved. NO REGRESSION otherwise holds: the kind=decision successor-extraction rule is carried intact and un-reopened; the clause-4 active-baseline citation rule still preserves rfc:0097/0027/0039/0041 (Falsifier 1 re-confirmed, D174 preserved via live RFC 0109); the latency machinery's core safety (recovery loop released, skip-on-overrun, L4 zero-writes-on-timeout / no torn write) is intact; G3 substrate is intact (runtime 0045_cullable_entity.sql under go/pkg/db/sql/, both read+write authority-inventory rows, GRANT SELECT,INSERT,UPDATE to striatumd_rw with no owner DDL/FK >=27, explicit-column reads / no SELECT *); G4 forward-compat is intact (OQ1 sweep/peer writer, (kind,ref) ON CONFLICT + extensible candidacy_state CHECK, the §6 deferral table; no smuggled P1+ tombstone/deletion/page/doctor-class/run-admission action). This is needs_revision, NOT reject: the architecture is sound, the substrate and forward-compat stay cleared, the decision-successor parser stays mechanical, the cursor-refresh carrier is genuinely fixed by design, and BOTH residuals are narrow and concrete — extend the static protected/frozen manifest to cover all tracked status:frozen records (docs/records/**) and re-derive the corpus so D081 nominates with zero counted hits (G1'''); and either declare+bound the non-cooperative-FS-hang cull-liveness degradation with coverage or add a stale-worker/generation fence + a non-cooperative-hang test (G2''' no-regression). Per the gate's exhaustion rule, both residuals route to the operator for a fresh -v5 rather than a further in-run revision."
findings:
  - id: F4-G1-EXPOSED-FROZEN-AUDIT-RECORD-WITHHOLDS-D081
    severity: critical
    posture: tier1_false_positive
    status: converted_to_constraint
    challenge: "The v4 clause-3 static protected/frozen manifest protects only docs/records/_frozen/** (HOLDER.md:401-408) but the live tree carries tracked status:frozen records OUTSIDE that root under docs/records/ — docs/records/INDEX.md plus 8 docs/records/audits/*.md (verified via git ls-files + head). docs/records/audits/STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md is tracked with front-matter `status: frozen` and is a front-matter-carrying docs/** artifact in the clause-4b inbound-scan set; under clause 4c (`status` not in {superseded,tombstoned,withdrawn,deprecated}, path not in the manifest) it is a LIVE source. Its D081 hits at :97/:323/:367-370 carry no 4f closure-lexicon token on the matched physical line (verified) and are not own-row/named-successor/reference-link/README-index, so they are counted active-baseline citations; >=1 counted hit sets reachable_from_root=true and withholds D081. The corpus therefore nominates at most {D267}, not {D267, D081} — the holder's claim that D081's only inbound hit is its own row (HOLDER.md:481-486) is source-false, the published true-positive set is non-derivable, and a frozen artifact is exposed as candidacy input. A two-implementer split: literal manifest -> count the hit -> {D267}; treat frozen records as provenance -> exclude -> {D267,D081}, but that exclusion is not encoded as a tree-local rule."
    closest_acceptable_answer: "Extend the static protected/frozen source set so ALL tracked status:frozen provenance is covered (e.g. protect docs/records/** instead of only docs/records/_frozen/**, or add a tree-local clause-4c rule that a front-matter status:frozen record is non-live), keeping docs/rfcs/ and docs/decisions/ eligible; then re-derive the G1 corpus with a protected-source fixture and a table-driven case for the audit file proving D081 is again nominated with ZERO counted inbound citations and the preserved set untouched, so two implementers read every corpus row identically."
    affected_invariants:
      - "RFC 0170 Acceptance Criterion 1: Tier-1 exact, two implementers read every corpus row identically with no LLM/external judgment"
      - "G1''' verification.gate: the static tree-local protected pathspec leaves no exposed frozen artifact; the corpus nominates exactly {D267, D081} with zero hits on the preserved set; rfc:0097/0027/0039/0041 + D174 preserved"
    source_refs: ["dialogue:2"]
  - id: F4-G2-NONCOOP-FS-HANG-OVERCLAIMS-FS-DEADLINE
    severity: high
    posture: readonly_safety_latency
    status: converted_to_constraint
    challenge: "The G2''' cursor-refresh-timing carrier is genuinely closed by option (b) (fold off the wait-gating path; verified against scheduler.go:55-80 / sweep.go:114,246-263 / doctor.go:383-390; B5 refresh-not-deferred binds). But the holder also re-asserts the credited latency machinery is unchanged, including a 'deadline over the filesystem scan' under which the detached goroutine self-terminates within DefaultCullFoldTimeout (HOLDER.md:640-650,657-675,820-825). The SPEC contradicts itself: HOLDER.md:657-659 admits context.WithTimeout cannot abort an in-flight blocking syscall, yet L3 claims 'even a scan stuck in a blocking syscall is abandoned by its own goroutine' and B5 assertion 6 requires the blocked goroutine to observe cullCtx.Done() and terminate at the timeout — achievable only for a COOPERATIVE hang, not the non-cooperative filesystem carrier the holder names. A non-cooperative filesystem hang then either permanently holds the single in-flight cull slot (every later tick skips the cull forever — undeclared cull-liveness failure until restart) or, if the slot is released on timeout, admits a stale late writer violating L4 compute-then-commit unless an unspecified generation/epoch fence is added; B5 as written cannot model this. Recovery/doctor SAFETY is preserved (the detached fold never blocks RunScheduler, defers the cursor refresh, or writes doctor inputs), so this is a no-regression/over-claim defect on a credited property, not a recovery-scheduler safety regression."
    closest_acceptable_answer: "Keep the off-path cursor-refresh repair. Discharge the no-regression gap with ONE of: (i) declare and bound the cull-liveness degradation explicitly (a non-cooperative FS hang disables the cull fold until daemon restart; recovery/doctor safety preserved) with a test proving the stuck worker is visible/logged and cannot write after timeout; (ii) add a stale-worker/generation fence so the timeout releases the cull slot for future ticks and any late result is rejected before the L4 write, proven by a non-cooperative-hang test that future cull ticks still run without stacking writers or torn cullable_entity writes; or (iii) constrain the scan to interruptible operations and prove it with a negative control that does not merely cooperate with ctx.Done(). Keep the credited deadline/watchdog/skip-on-overrun/L4 compute-then-commit machinery unchanged."
    affected_invariants:
      - "C-G2 verification.gate: the credited per-tick deadline over BOTH the DB read and the filesystem scan, with the detached scan goroutine bounded/self-terminating, is preserved (no-regression)"
      - "B4/B5: a blocked cull scan is bounded and cannot silently disable future cull ticks or admit a stale write"
    source_refs: ["dialogue:3"]
constraints:
  - id: C-G1-PROTECTED-FROZEN-PATHSPEC-COMPLETENESS
    source_finding: F4-G1-EXPOSED-FROZEN-AUDIT-RECORD-WITHHOLDS-D081
    posture: tier1_false_positive
    severity: critical
    kind: gate
    binding: true
    text: "Discharged in part (the open-issue/external-state term is removed; the pathspec is fully static and tree-local; docs/rfcs/ and docs/decisions/ are eligible; D267 derives; the preserved RFC set + D174 is untouched). Residual to v5: the static protected/frozen manifest protects only docs/records/_frozen/** and leaves tracked status:frozen records under docs/records/ (INDEX.md + docs/records/audits/*.md) exposed as live clause-4 sources, so the named audit's counted D081 citations withhold D081 and the corpus no longer nominates exactly {D267, D081}. Extend the protected/frozen source set to cover all tracked status:frozen provenance (protect docs/records/** or add a tree-local status:frozen-record non-live rule to clause 4c), keep docs/rfcs/ and docs/decisions/ eligible, and re-derive the corpus with a fixture + table-driven case proving D267 AND D081 nominate with zero counted hits, the audit file is excluded as a source, and the preserved set is untouched."
    source_refs: ["dialogue:2"]
    verification:
      gate: "G1 corpus test over the live tree under the completed static protected/frozen manifest: the predicate nominates exactly {decision:D267, decision:D081}; a protected-source fixture proves docs/records/** (incl. docs/records/audits/STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md) and docs/records/_frozen/** classify reachable_from_root=true (excluded as live clause-4 sources) while docs/rfcs/ and docs/decisions/ stay eligible; zero hits on the preserved set (rfc:0097/0027/0039/0041, D174); no path classification depends on external (open-issue) state."
    final_review_required: true
  - id: C-G2-NONCOOP-FS-HANG-NO-REGRESSION
    source_finding: F4-G2-NONCOOP-FS-HANG-OVERCLAIMS-FS-DEADLINE
    posture: readonly_safety_latency
    severity: high
    kind: gate
    binding: true
    text: "Discharged in part (the cursor-refresh-timing carrier is removed by construction via the off-the-wait-path fold; B5 refresh-not-deferred binds and fails with the fold back on the wait-gating path; recovery/doctor safety and no-torn-write are intact). Residual to v5: the no-regression of the credited 'deadline over the filesystem scan / detached-goroutine self-termination' property — a non-cooperative filesystem hang is not aborted by cullCtx (HOLDER.md:657-659), so it either permanently occupies the single cull slot (silent cull-liveness failure) or admits a stale late writer, and B5 only models a cooperative hang. Either declare+bound the cull-liveness degradation explicitly with coverage, OR add a stale-worker/generation fence that releases the slot and rejects late writes, OR constrain the scan to interruptible operations — proven by a non-cooperative-hang test (not one that merely cooperates with ctx.Done()). Keep the off-path cursor-refresh repair and the credited deadline/watchdog/skip-on-overrun/L4 compute-then-commit machinery unchanged."
    source_refs: ["dialogue:3"]
    verification:
      gate: "Non-cooperative-hang regression test in go/pkg/recovery (a DecayTickSweep scan that neither panics nor cooperates with ctx.Done()): assert the chosen discharge holds — either (declared degradation) the stuck worker is visible/logged, performs no write after timeout, and the cull fold's disablement is bounded/observable; or (fence) the cull slot is released for the next tick, the late result is rejected before the L4 write, and future cull ticks still run without stacking writers or torn cullable_entity writes. The cursor-refresh repair, recovery safety, and no-torn-write properties remain byte-identical to v4."
    final_review_required: true
branches:
  tier1_false_positive: "blocked"
  readonly_safety_latency: "blocked"
  substrate_correctness: "cleared"
  forward_compatibility: "cleared"
---

# Collaboration Ledger — RFC 0170 P0 design v4 (cycle 1, the single revision round)

**Verdict: `needs_revision`.** This is v4's single proper revision round, so the
gate is now **exhausted**: the residual constraints route to the **operator for a
fresh `-v5`** run. The committer does **not** publish the consolidated SPEC and
**D271 is not ratified** this run.

The revision made **real, creditable progress on both residuals** — the
external-state defect in clause 3's pathspec is fully closed (it is now a static,
tree-local checked-in manifest with no open-issue/GitHub term), and the G2'''
cursor-refresh-timing carrier is genuinely **removed by construction** (the cull
fold is moved off the scheduler's `wait`-gating path, source-verified). But
**G1''' is not fully discharged** (a new, in-tree corpus defect exposes a frozen
audit record and makes `decision:D081` non-derivable), and a **no-regression
defect** stands against the credited latency machinery (the over-claimed "deadline
over the filesystem scan"). The other credited/cleared machinery (the
`kind=decision` successor rule, the clause-4 preserved set, **G3**, **G4**) is
confirmed **un-regressed**.

## Per-residual disposition (the revision gate)

| Binding residual | Result | Basis |
|---|---|---|
| **G1''' — fully static, tree-local protected pathspec; corpus nominates exactly `{D267, D081}`** | **NOT DISCHARGED** | Falsifier 1 STANDS. The open-issue/external-state term **is** removed (credited), `docs/rfcs/` and `docs/decisions/` are eligible, `D267` derives, and `rfc:0097/0027/0039/0041` + `D174` are untouched. But the static manifest protects only `docs/records/_frozen/**`, leaving ≥9 tracked `status: frozen` records under `docs/records/` (verified: `INDEX.md` + 8 `docs/records/audits/*.md`) exposed as **live** clause-4 sources. `docs/records/audits/STRIATUM_DECISION_RECORD_AUDIT_OPUS_4_8_2026-06-16.md` (`status: frozen`, tracked) carries 4f-surviving `D081` hits at `:97/:323/:367-370` (no closure token on the matched line — verified), so clause 4 fires, `D081` is withheld, and the corpus nominates at most `{D267}`. The holder's "D081's only inbound hit is its own row" (HOLDER.md:481-486) is source-false. **Exposed frozen artifact + corpus ≠ `{D267, D081}` = G1''' UNMET.** |
| **G2''' — close the cursor-refresh-timing carrier** | **CARRIER DISCHARGED; a no-regression defect STANDS** | Falsifier 2 credits the carrier removal: option (b) (fold off the `wait`-gating path) is source-true (`scheduler.go:55-80` / `sweep.go:114,246-263` / `doctor.go:383-390`), and B5's refresh-not-deferred assertion binds (fails with the fold back on the `wait`-gating path). **But** the holder over-claims the credited "deadline over the filesystem scan": HOLDER.md:657-659 admits `context.WithTimeout` cannot abort an in-flight blocking syscall, while L3/B5 assertion 6 claim the blocked goroutine self-terminates at the timeout — true only for a *cooperative* hang. A non-cooperative FS hang permanently holds the single cull slot (silent cull-liveness failure) or admits a stale late writer; B5 cannot model it. Recovery/doctor **safety** is preserved, but the **no-regression** property is not. |

## Per-cleared/credited-property disposition (no regression)

| Property | Result | Basis |
|---|---|---|
| **`kind=decision` successor-extraction rule** | **STILL MET** | Carried intact, not reopened; `D267→{D270}`, `D081→{D087,D094,D104}`, bare-`superseded` withheld. Falsifier 1 re-confirms it derives `D267` and the successor of `D081`. |
| **Clause-4 active-baseline citation rule** (preserves `rfc:0097`/`0027`/`0039`/`0041`, `D174`) | **STILL MET** | §3 clause 4 carried intact; Falsifier 1 re-confirms the preserved RFC set + `D174` (live RFC 0109) untouched by the new challenge. |
| **Latency machinery — core safety** (recovery loop released, skip-on-overrun, L4 zero-writes-on-timeout / no torn write) | **STILL MET** | Carried in §4; the off-path fold strictly strengthens "recovery loop never blocked"; no torn write on a timed-out/blocked tick. *(The "deadline over the non-cooperative FS scan" sub-claim is the standing G2''' no-regression residual above.)* |
| **G3 — substrate correctness** | **STILL MET** | §2 intact: `0045_cullable_entity.sql` under `go/pkg/db/sql/`, both read+write authority-inventory rows, `GRANT SELECT,INSERT,UPDATE` to `striatumd_rw` with no owner DDL/FK (≥27), explicit-column reads (no `SELECT *`). |
| **G4 — forward-compatibility** | **STILL MET** | §5/§6 intact: OQ1 sweep/peer writer; `(kind,ref)` ON CONFLICT + extensible `candidacy_state` CHECK; the P0/P1+ deferral table. No smuggled P1+ action. |

## Per-falsifier disposition

- **Falsifier 1 — Tier-1 exactness / false-positive lens (dialogue:2): MATERIAL,
  STANDING.** The external-state pathspec defect is genuinely closed, but the
  static manifest's failure to protect `status: frozen` records living outside
  `docs/records/_frozen/` newly exposes them as live clause-4 sources, and the
  named audit's counted `D081` citations make the published `{D267, D081}`
  non-derivable. Grounded in real, verified paths/refs. **Not rebutted → `C-G1`
  residual.**
- **Falsifier 2 — read-only safety / no-regression lens (dialogue:3): MATERIAL,
  STANDING (the no-regression sub-point only).** The cursor-refresh-timing carrier
  is correctly removed by the off-path design (credited). The standing gap is the
  over-claimed "deadline over the *filesystem* scan": a non-cooperative syscall
  hang is not aborted by `cullCtx` (the SPEC self-contradicts), so it can silently
  disable future cull ticks or admit a stale write, and B5 can only model a
  cooperative hang. Recovery/doctor safety and G3/G4 un-regressed. **Not rebutted →
  `C-G2` residual.**

## Gate outcome — exhausted, route to `-v5`

Neither binding residual is fully discharged (G1''' UNMET; G2''' carrier met but a
credited-property no-regression defect stands), so this is **not** a clearing
verdict. Because v4 is the single allowed revision round, a `needs_revision` here
**exhausts the gate**; per the SEED's gate rule the residuals route to the
**operator for a fresh `-v5`** rather than a further in-run revision (do **not**
ratchet, do **not** commit the consolidated SPEC, do **not** ratify D271).

Both residuals are narrow and in-P0-buildable:

- **G1''' (`C-G1-PROTECTED-FROZEN-PATHSPEC-COMPLETENESS`):** keep the now-static
  tree-local pathspec; extend the protected/frozen source set to cover **all**
  tracked `status: frozen` provenance (protect `docs/records/**`, or add a
  tree-local clause-4c rule that a front-matter `status: frozen` record is
  non-live), keeping `docs/rfcs/` and `docs/decisions/` eligible; then re-derive
  the corpus with a fixture + table-driven case so `D267` **and** `D081` nominate
  with zero counted hits and the preserved set is untouched.
- **G2''' (`C-G2-NONCOOP-FS-HANG-NO-REGRESSION`):** keep the credited off-path
  cursor-refresh repair and the deadline/watchdog/skip-on-overrun/L4 machinery;
  resolve the non-cooperative-filesystem-hang gap by **declaring and bounding** the
  cull-liveness degradation with coverage, **or** adding a stale-worker/generation
  fence (timeout releases the slot, late writes rejected) proven by a
  non-cooperative-hang test, **or** constraining the scan to interruptible
  operations with a non-cooperating negative control.

This is `needs_revision`, **not `reject`** — the architecture is sound, the
substrate and forward-compat stay cleared, the decision-successor parser stays
mechanical, the cursor-refresh carrier is genuinely fixed by design, and only a
complete static frozen-record manifest (G1''') and an honest filesystem-hang
no-regression story (G2''') remain.
