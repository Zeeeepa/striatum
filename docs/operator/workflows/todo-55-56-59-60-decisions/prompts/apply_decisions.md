# Apply Recorded Decisions

Read the work packet first and use the exact `author:` line supplied in
`expected_artifacts`.

Proceed only if the human-principal checkpoint has been resolved with recorded
decision ids. Apply those decisions to TODO/roadmap/operator state. If a
decision is missing or ambiguous, block instead of guessing.

Do not implement source behavior unless a recorded decision explicitly makes it
in scope for this workflow.
