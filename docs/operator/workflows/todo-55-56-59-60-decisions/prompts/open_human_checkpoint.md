# Open Human Checkpoint

Read the work packet first.

Open a human-principal checkpoint using the packet's exact `block` command
shape. Use `--severity human_checkpoint`, `--kind owner_decision`, and a
description that points to
`docs/operator/artifacts/todo-55-56-59-60-decisions/checkpoint/CHECKPOINT_BRIEF.md`.

Do not write or publish a product decision artifact. The human principal must
record decisions with `striatum decision record` and the operator will resolve
the checkpoint.
