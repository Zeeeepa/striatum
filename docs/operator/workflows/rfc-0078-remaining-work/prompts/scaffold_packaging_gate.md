# Scaffold Go-Only Packaging Gate

Create or verify the dedicated RFC 0078 packaging/release workflow under
`docs/operator/workflows/rfc-0078-go-only-packaging-release/` plus the plan at
`docs/operator/plans/rfc-0078-go-only-packaging-release.md`.

The scaffold must cover version source, Go module/root layout decision,
Makefile/CI/release archives, Go embed assets, smoke scripts, install docs,
and PyPI retirement.
