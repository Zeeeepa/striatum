# Scaffold Workflow/Artifact Parity Gate

Create or verify the dedicated RFC 0078 workflow/artifact parity workflow under
`docs/operator/workflows/rfc-0078-workflow-artifact-parity/` plus the plan at
`docs/operator/plans/rfc-0078-workflow-artifact-parity.md`.

The scaffold must cover artifact contracts, front-matter parser parity,
workflow Validate parity, lint parity, generator reuse, template catalog
ownership, render-md decision, tests, and final summary.
