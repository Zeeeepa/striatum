# Scaffold Go CLI RPC Router Gate

Create or verify the dedicated RFC 0078 Go CLI RPC router workflow under
`docs/operator/workflows/rfc-0078-go-cli-rpc-router/` plus the plan at
`docs/operator/plans/rfc-0078-go-cli-rpc-router.md`.

The scaffold must cover contract-driven route generation, daemon RPC client,
local authoring commands, command retirement ledger, tests, and final summary.
