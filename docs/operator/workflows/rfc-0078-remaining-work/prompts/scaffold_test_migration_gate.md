# Scaffold Python Test Migration Gate

Create or verify the dedicated RFC 0078 test migration workflow under
`docs/operator/workflows/rfc-0078-python-test-migration/` plus the plan at
`docs/operator/plans/rfc-0078-python-test-migration.md`.

The scaffold must cover pytest coverage ledger refinement, Go integration
harness, CLI tests, web tests, workflow/artifact tests, corpus/archive tests,
packaging smoke replacement, and final deletion readiness.
