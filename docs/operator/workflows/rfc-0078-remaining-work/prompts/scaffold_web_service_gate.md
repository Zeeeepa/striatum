# Scaffold Go Web/Service Gate

Create or verify the dedicated RFC 0078 Go web/service workflow under
`docs/operator/workflows/rfc-0078-go-web-service-cutover/` plus the plan at
`docs/operator/plans/rfc-0078-go-web-service-cutover.md`.

The scaffold must cover route retention decisions, Go local service security,
static/template embedding, SSE, retained route tests, route retirement tests,
and final summary.
