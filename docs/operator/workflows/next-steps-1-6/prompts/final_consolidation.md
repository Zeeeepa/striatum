# Final Consolidation

Produce the expected synthesis artifact after all six track artifacts exist.

Consolidate the findings into:

- landed source/docs changes by track;
- workflow run status and run id;
- validation commands and outcomes;
- residual blockers;
- the next ordered TODOs.

Use valid `striatum.synthesis.v1` front matter and set the `author:` line to
the exact expected artifact author line from the work packet.
