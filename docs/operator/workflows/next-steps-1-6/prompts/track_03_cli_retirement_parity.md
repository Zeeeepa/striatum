# Track 3: CLI Retirement And MCP/UI Parity

Produce the expected synthesis artifact for this track.

Build or summarize the current CLI cutover ledger. The artifact must cover:

- remaining live workflow-control CLI verbs;
- which are replaced by MCP/UI, bootstrap, diagnostics, or temporary
  compatibility;
- missing parity that blocks hiding or retirement;
- tests and guardrails needed before any demotion;
- any blockers or follow-up work.

Use valid `striatum.synthesis.v1` front matter and set the `author:` line to
the exact expected artifact author line from the work packet.
