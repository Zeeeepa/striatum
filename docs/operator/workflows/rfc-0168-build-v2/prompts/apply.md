Apply the accepted review by producing the final synthesis artifact and source
state for RFC 0168 P0 v2. Keep the implementation on current `main`, preserve
RFC 0171 generated-records/docket files, keep runtime migration numbering after
`0046_generated_records.sql`, and explicitly close any accepted review findings
around F1 return/scrub/proof, F2 generation enforcement, F3 leased-uid worktree
grants, and D272 provider-credential selector discrimination.

Publish the required SUMMARY.md with files changed, assertions discharged, tests
run, review findings addressed, and residual operator work.
