# Finalize TODO 61-62 Cleanup

Read the work packet first and use the exact `author:` line supplied in
`expected_artifacts`.

Read the implementation handoff and both review artifacts. Produce a final
summary that names:

- cleanup accepted into the worktree,
- tests run and their result,
- unresolved blockers,
- TODO items that remain active,
- the next workflow the operator should queue.

Do not modify source files.
