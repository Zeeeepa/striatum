# Item 2: Legacy SQLite Cleanup

Delete or convert a small stale skipped-test batch that only exists for the
retired legacy SQLite runtime. Update guardrails and status docs so the
remaining debt narrows rather than moves around.
