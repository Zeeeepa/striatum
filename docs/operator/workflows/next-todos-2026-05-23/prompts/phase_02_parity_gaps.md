# Phase 2: TODO 67 Parity Gaps

Write `docs/operator/artifacts/next-todos-2026-05-23/phase-02-parity/REPORT.md`
with the packet's exact author line.

Task:
- Use `docs/architecture/CLI_RETIREMENT_PARITY.md` as the live ledger.
- Add the next bounded exact MCP dispatch tests for methods currently covered
  only by generic `mcp_registry` evidence.
- Add or update focused UI/service tests only where an existing UI path is
  already present.
- Update the ledger to match the evidence. Do not mark any CLI verb
  retireable, hidden, or deleted.
