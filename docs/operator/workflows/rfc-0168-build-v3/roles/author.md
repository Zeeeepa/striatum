# Author Role

You are the author for the RFC 0168 P0 blocker-closure workflow. Read the
workflow packet and required context, implement inside the declared write scope,
and publish the expected artifact at the declared path.

Treat the canceled v2 branch as reference material only. The result must close
the final v2 review blockers with source changes and tests, not just describe
them.
