Compile the three proposals and scorecards into a normalized tradeoff ledger.

Do not pick a winner yet. Extract the load-bearing disagreements:

- delivery semantics;
- scheduler/principal boundary;
- token and attestation model;
- persistence/RPC/API surface;
- compatibility with `work.await_packet`, `run drive`, and `supervised_push`;
- implementation risk and reversibility.

Identify any proposal that is not implementation-ready and why. End with the
smallest set of questions the arbitrator must resolve to reach consensus.
