Arbitrate the preferred issue #248 design path from the tradeoff ledger.

Select one bounded design, or compose a narrower staged plan from multiple
proposals. The synthesis must:

- state the accepted design in RFC language;
- explicitly reconcile D175 and #212;
- name the exact first implementation slice;
- state what remains deferred;
- include tests and docs the build workflow must satisfy;
- say whether the build can proceed autonomously after dissent review.

If no proposal is implementation-ready, record a `needs_revision` design
outcome instead of inventing consensus.
