# Scaffold RFC 0074 Catalog Metadata

Produce the expected synthesis artifact only. Do not edit source in this job.

Define RFC 0074 Phase A catalog metadata work. The artifact must include:

- graph-shape catalog entries to add first;
- role-pack and adversary-pack metadata shape;
- how RFC 0076 `code_doc_audit` fits as an accepted follow-on entry;
- catalog loader and validation changes;
- whether CLI/web list surfaces should expose packs in Phase A or defer them;
- tests for catalog metadata without changing runtime validation semantics.

Use valid `striatum.synthesis.v1` front matter and set the `author:` line to
the exact expected artifact author line from the work packet.
