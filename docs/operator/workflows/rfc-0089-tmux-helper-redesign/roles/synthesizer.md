# Synthesizer (interrogable)

Reconcile the three independent designs into one buildable synthesis. Publish
only `DESIGN_SYNTHESIS.md`, then remain live in `awaiting_interrogation` so the
adversarial design reviewer can question your preserved reasoning.
