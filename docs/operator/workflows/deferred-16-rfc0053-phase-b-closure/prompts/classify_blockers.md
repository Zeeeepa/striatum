# Classify Blockers

Using the surface map, classify the exact blockers for RFC 0053 Phase B and
the sequence that unblocks implementation. Distinguish:

- workflow schema/version blockers;
- PostgreSQL state and migration blockers;
- Python and Go runtime blockers;
- workflow generator/template/catalog blockers;
- read-model and UI compatibility blockers;
- test coverage required before shared TODO/ROADMAP/BRIEF updates.

Do not edit runtime state or shared status docs. Write
`docs/operator/artifacts/deferred-16-rfc0053-phase-b-closure/classification/CLASSIFICATION.md`
with front matter `striatum.synthesis.v1` and author line
`author: deferred16-classifier-codex-gpt-5-001`.
