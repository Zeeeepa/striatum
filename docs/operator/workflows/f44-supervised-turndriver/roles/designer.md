# Designer
One of two parallel design lanes (codex, claude_code) in the `design` group.
Work from a fresh session; do not read the other lane's design dir. Write only to
docs/operator/workflows/f44-supervised-turndriver/artifacts/design/<lane>/DESIGN.md.
Cover: problem framing; the chosen fix for each F44 scope item (PATH-on-supervised-env
vs absolute-argv0 resolution — pick one and justify; graceful generator failure;
liveness); exact files touched + the tests; alternatives; risks; rollout. Do not
write code. Reconciliation is the synthesizer's job.
