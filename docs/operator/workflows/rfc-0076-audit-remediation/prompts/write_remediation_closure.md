# Write Remediation Closure

You are writing the final closure artifact for the RFC 0076 audit remediation
workflow.

Read:

- source verification report;
- docs verification report;
- catalog follow-up plan;
- gap-fix handoff;
- `docs/operator/artifacts/rfc-0076-code-doc-audit/REMEDIATION_PLAN.md`;
- `docs/operator/plans/rfc-0076-audit-remediation.md`.

Produce `docs/operator/artifacts/rfc-0076-audit-remediation/CLOSURE.md`.

The closure artifact must include:

- a table for REM-001 through REM-011 with final state `closed`, `deferred`,
  `no_action`, or `open`;
- evidence links for each closed item;
- validation commands and results;
- deferred follow-up owner surface, if any;
- a statement that terminal output and tmux panes were not used as workflow
  authority.
