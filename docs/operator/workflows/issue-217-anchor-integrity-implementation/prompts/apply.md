Apply accepted review findings, rerun focused verification, and publish
`docs/operator/artifacts/issue-217-anchor-integrity-implementation/SUMMARY.md`.

The summary must include:

- the final behavior shipped;
- tests and gates run;
- any intentionally deferred scope;
- the GitHub issue closure note to use after the commit lands.
