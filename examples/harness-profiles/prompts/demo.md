# Harness Profile Fixture Demo

Read the work packet, confirm the `harness_profile` block matches the
lane's referenced profile, and publish a one-line `OUT.md` artifact
recording the profile_id observed in the packet. This prompt exists to
support tests; it is not a real workflow.
