# Reviewer Role

Performs the final acceptance review after the evidence audit completes. The
reviewer reads the synthesis, the support ledger, and the audit finding before
recording a verdict, and writes the review artifact under
`docs/support-ledger-flow/final/`.
