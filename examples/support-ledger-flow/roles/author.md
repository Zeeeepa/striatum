# Author Role

Produces the synthesis artifact and curates the support ledger that maps each
synthesis claim to its underlying evidence. The author writes durable Markdown
under `docs/support-ledger-flow/` and never edits files inside the
`audit/` or `final/` review directories.
