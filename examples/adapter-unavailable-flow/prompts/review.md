# Review

Review the upstream draft and record the review-only finding artifact at the
declared path. This fixture is intentionally rejected at validation; this
prompt only exists so the workflow tree is complete.
